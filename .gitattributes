# سطور ويندوز إلزاميّة لملفّات الأوامر.
# قِيس 2026-09-01: ملفّ .bat بنهايات LF ينكسر عند cmd على الكتل
# (if (...) و for)، فتُنفَّذ أسطر REM كأوامر ويبدو الزرّ ميتًا.
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

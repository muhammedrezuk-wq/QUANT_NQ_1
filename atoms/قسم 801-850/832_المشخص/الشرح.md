# System Diagnostician

> أُنشئ هذا الملف آليًا خلال نافذة الإصلاح (م-29، ورقة ٤١، 2026-08-28) — محتواه بالكامل مقيس من `manifest.yaml`، لا سرد يدوي بعد.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٨٣٢` |
| الاسم | System Diagnostician |
| المجلّد | `832_المشخص` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `system.alert.state`
- `measurement.health.state`
- `measurement.latency.state`
- `market.regime.state`
- `drift.vector.state`
- `adaptation.kill_switch.state`
- `decision.behaviour.telemetry`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `system.diagnosis.state`

<!-- BITAQA_END -->

# File Cleanup Manager

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٨٠٣` |
| الاسم | File Cleanup Manager |
| المجلّد | `803_تنظيف_الملفات` |
| الإصدار | `2.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٦٠٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_DAY`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `tools.file_cleanup.completed`
- `tools.file_cleanup.failed`

**إعداداتها:** `scan_dirs` · `older_than_days` · `patterns` · `interval_days`

<!-- BITAQA_END -->

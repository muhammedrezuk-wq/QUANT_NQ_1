# Change Governor

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٨٥٠` |
| الاسم | Change Governor |
| المجلّد | `850_حاكم_التغيير` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `recalibration.proposed`
- `adaptation.kill_switch.state`

**تُرسِل (من `manifest.yaml`):**
- `experiment.state`
- `adaptation.limits.state`

**إعداداتها:** `min_evidence_windows` · `max_change_per_step` · `max_change_per_day` · `max_active_experiments` · `min_dwell_s` · `cooldown_s` · `max_changes_per_window`

<!-- BITAQA_END -->

# File Archive Manager

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٨٠٢` |
| الاسم | File Archive Manager |
| المجلّد | `802_أرشفة_الملفات` |
| الإصدار | `2.2.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٦٠٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_DAY`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `tools.file_archive.completed`
- `tools.file_archive.failed`

**إعداداتها:** `archive_dir` · `source_dirs` · `older_than_days` · `interval_days`

<!-- BITAQA_END -->

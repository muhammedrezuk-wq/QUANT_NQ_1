# التاريخ — 802_أرشفة_الملفات

**الذرّة:** `٨٠٢` · **الإصدار:** `2.2.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.2.1` | إضافة مجلد جسر cTrader ‏(`C:\Users\NQ\Documents\cTrader\User Files\quant_nq_bridge`) إلى `source_dirs` — **إعدادات فقط، صفر تغيير كود**. السبب المقيس: ملفات الجسر المدوّرة (`ctrader_bridge.jsonl.YYYYMMDD_HHMMSS`) تتراكم بلا أي ذرّة تغطّيها — 328 MB مدوّرات يوم القياس (+~50 MB/يوم)، والـcBot يدوّر ولا ينظّف أبدًا، و617 لا يقرأ المدوّرات إطلاقًا (مُثبَت بفحص الكود). الملف الحي محميّ تلقائيًّا بفلتر عمر آخر تعديل. العتبات القائمة كما هي (`older_than_days: 30`، `interval_days: 7`) — لا أرقام جديدة |
| ٢٠٢٦-٠٨-١٨ | `2.2.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 60` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

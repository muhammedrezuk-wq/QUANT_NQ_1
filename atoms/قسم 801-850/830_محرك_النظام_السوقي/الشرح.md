# Market Regime Engine

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٨٣٠` |
| الاسم | Market Regime Engine |
| المجلّد | `830_محرك_النظام_السوقي` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `structure.trend.state`
- `analysis.volatility.state`

**تُرسِل (من `manifest.yaml`):**
- `market.regime.state`

**إعداداتها:** `entry_threshold` · `exit_threshold` · `confirmation_window` · `min_duration_s` · `volatility_high`

<!-- BITAQA_END -->

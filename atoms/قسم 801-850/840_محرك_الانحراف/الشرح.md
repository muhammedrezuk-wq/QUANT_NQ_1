# Drift Engine

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٨٤٠` |
| الاسم | Drift Engine |
| المجلّد | `840_محرك_الانحراف` |
| الإصدار | `1.0.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `measurement.latency.state`
- `measurement.health.state`
- `market.regime.state`

**تُرسِل (من `manifest.yaml`):**
- `drift.vector.state`
- `recalibration.proposed`

**إعداداتها:** `warmup_windows` · `drift_threshold` · `proposal_cooldown_windows`

<!-- BITAQA_END -->

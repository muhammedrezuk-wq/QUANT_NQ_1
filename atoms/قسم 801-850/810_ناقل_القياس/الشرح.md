# Telemetry Carrier

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٨١٠` |
| الاسم | Telemetry Carrier |
| المجلّد | `810_ناقل_القياس` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `telemetry.batch.closed`
- `telemetry.carrier.state`

**إعداداتها:** `out_dir` · `batch_size` · `flush_interval_s` · `max_files` · `max_buffer` · `stream_prefixes`

<!-- BITAQA_END -->

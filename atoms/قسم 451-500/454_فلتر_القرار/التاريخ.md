# التاريخ — 454_فلتر_القرار

## 2.4.0 — 2026-08-20
- **عقد الحاجز الكامل + معالجة المجهول + الهوية — بختم NQ بند 22 حزمة ب (ب١/ب٦/ب٧):**
  - **ب٧:** كل حاجز يعلنه 454 (بوابة الدرجة والجانب، الفلاتر الستة بغائبها وبائتها ومغاير دورتها وفاشلها، التقويم، نافذة الأخبار، جودة السوق، التغذية) يحمل الرباعية `value`/`threshold`/`reason`/`measured_at` بقائمة `barriers` الموحدة أعلى الحمولة — بترتيب `blocked_by` نفسه، و`blocked_by` القائم بقي كما هو توافقًا. القيمة/العتبة `None` حيث لا قياس/عتبة فعليين عند 454 (لا اختراع)؛ للبائت قياس حقيقي (العمر مقابل `filter_ttl_s`) وللبوابة (`الدرجة مقابل min_score`). أوقات القياس ساعة حائط تخزن لحظة ورود كل حالة (التقويم/الجودة/التغذية/الأخبار/الفلاتر) و`seen_at` الرتيب بقي لحساب المهلة وحده.
  - **ب٦:** جانب القرار من `decision_side` (455→458 هو الحاسم) وقديمه `signal` توافقًا وneutral القديم = انتظار. المجهول ليس معلومًا: جانب غائب يحجب بـ`decision_side_unknown` (لا «انتظار» مزعوم)، ودرجة غائبة تحجب بـ`score_unknown` بدل التحول 0 واجتياز عتبة 0 السارية بالصدفة — والدرجة تنشر بقيمتها المقيسة أو `None` (لا صفر مصبوب).
  - **ب١ (حكم ق٩ §١٧):** الهوية الست تمرّ كاملة بالمخرج مع `identity_missing` وإنذار `identity_incomplete` للناقص (الإنذار لا يحجب — الحجب بقائمة الحواجز وحدها).
  - اختبارات: رباعية بوابة الدرجة ومطابقة القائمة لـ`blocked_by` + رباعية نافذة الخبر + المجهولان (درجة/جانب) + المرور بالهوية وdecision_side — 12/12.

## 2.3.2 — 2026-08-20
- **حاجز نافذة الأخبار بختم NQ بند 22 حزمة أ — ق٧:** اشتراك جديد بـ`news.trading_window.state` (من 411). `block=true` ضمن نافذته ⇒ `blocked_by` يتضمّن `news_window` مع التفاصيل (`grade`/`phase`/`window_end`) بالميتاداتا — بنفس نمط حاجز التقويم القائم. النوافذ تُخزَّن لكل رمز وتُشذَّب آليًّا بانقضاء `window_end` (الحاجز يزول بانتهاء النافذة بلا حدث إلغاء)، وغياب النوافذ لا يحجب — الحجب حصرًا ضمن نافذة معلنة. عداد النوافذ الحيّة ظاهر بالصحّة.

## 2.3.1 — 2026-08-20
- **تنظيف لغة الكود (م3/ملف4 — عربي داخل atom.py):** حذف ٧ مواضع عربية من `atom.py` (تعليق العيارات، مخزن أحكام كل دورة، جسر مفاتيح جودة السوق، docstring دلاء `_cycle_buckets`، حسم الأحكام بالمعرّف، إعلان سبب سقوط البوّابة، سلال الحجب بالصحّة) — مضمونها كله مسجّل في بنود `2.3.0`/`2.2.0`/`2.1.1`/`2.1.0`/`2.0.1` أدناه. صفر تغيير في المنطق.

**الذرّة:** `٤٥٤` · **الإصدار:** `2.3.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.3.0` | **عيارا المالك من اللوحة:** `min_score` و`filter_ttl_s` مُعامِلان محكومان بسجلّ المُعامِلات ([[shared/decision_dials.py]]) — قيمة المالك المعتمدة تعلو المانيفست، التعديل عبر ٩٠١ بتأكيد اللوحة، تطبيق حي + نشر `decision.settings.state`. (أرضية `MIN_FILTER_TTL_S=0.1` تبقى حارس معنى لا سقفًا). |
| ٢٠٢٦-٠٨-١٩ | `2.2.0` | **مخزن أحكام لكل دورة بذاتها:** خانة «آخر حكم فقط» كانت تُداس بحكم الدورة التالية قبل أن يصل قرار الدورة الحالية عبر خط الأنابيب (452←453←458)، فكلما سرُعت الدورات حُكم على كل قرار «دورة مغايرة» (`mism=` على الفلاتر الأربعة، `passed=0` — مقيس حيًّا بعد شفاء ذراع الاستراتيجيات ورجوع الدورات لسرعتها). صار حكم كل دورة يُحفظ بمعرّفها ويُشذَّب بمهلة النضارة القائمة (لا رقم جديد)؛ «دورة مغايرة» صارت تعني: للفلتر دلوٌ موجود وليس فيه هذه الدورة. حارس جديد: قرار متأخر بالأنبوب يجد أحكام دورته (٥/٥). |
| ٢٠٢٦-٠٨-١٩ | `2.1.1` | **جسر مفاتيح الحساب للفلاتر الفرعية:** بعض الناشرين (مثل 468 التحكم بالأصول) يرسلون حكمهم باسم الحساب الحقيقي والقرار يصل بلا حساب — فلا يلتقيان أبدًا ويبقى الفلتر «غائبًا» إلى الأبد (`miss=asset_filter` مقيسة حيًّا). `_records()` تأخذ الآن سجلات أي حساب لنفس (الرمز، الإطار) كقاعدة تدوسها المفاتيح الأدق؛ صحّة الربط يحرسها تطابق `cycle_id` وTTL كما هما. |
| ٢٠٢٦-٠٨-١٩ | `2.1.0` | **جسر مفاتيح الحساب لجودة السوق:** 115 تنشر الجودة باسم الحساب الحقيقي والقرار بلا حساب → `market_quality_unknown` حظرٌ دائم (مقيس حيًّا). عند غياب المفتاح الدقيق تُؤخذ **أسوأ** حالة مخزّنة للرمز عبر كل الحسابات — الفشل-المغلق محفوظ (أي INVALID يحجب). |
| ٢٠٢٦-٠٨-١٩ | `2.0.1` | **رؤية الحجب:** آخر قائمة حجب تُعلن في رسالة الصحّة بسلالها (غائب/بائت/دورة مغايرة/فاشل/تشغيلي) بدل الدفن في حمولة لا تصل اللوحة، وسبب سقوط بوابة الدرجة (`signal_wait`/`score_gate`) يُعلن مع الحُجّاب — «لماذا لا تمرّ القرارات» يُقرأ من اللوحة مباشرة (عين المالك). صفر تغيير في حكم المرور. |
| ٢٠٢٦-٠٨-١٨ | `2.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

- 2026-08-25 · v2.5.0 — filter verdicts match by scope + freshness (DECISION_FILTER_TTL_S dial) instead of exact tick-cycle equality; FILTER_CYCLE_MISMATCH was a top measured blocker while cycles churn per tick. Verdict cycle ids are declared in metadata.verdict_cycles. (nq seal, room rebuild)

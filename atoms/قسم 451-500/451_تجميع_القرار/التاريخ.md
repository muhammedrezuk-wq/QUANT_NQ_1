# التاريخ — 451_تجميع_القرار

## 2.8.1 — 2026-08-20
- شفافية الصحة: `new_duplicates` كان يُنزل الحالة «متراجعة» دون أن يظهر في رسالتها (مقاس حيًّا: متراجعة وكل المطبوع أصفار، والتفاصيل لا تعبر واجهة النواة) — أضيف `new_dup=` للرسالة. لا تغيير منطق.

## 2.8.0 — 2026-08-20
- **بختم NQ بند 22 حزمة ب — توصيل الثمانية (عمق الدورة + حالتها المجمعة):**
  - **العمق المجمع:** حمولة الدورة تحمل `current_depth`/`required_depth` = متوسط موزون بنمط دمج المسارين المختوم نفسه (أوزان المساهمة القائمة `weight_effect` فوق صفوف `weight_known`) مقصورًا لكل حقل على الأدلة التي **قاست** ذلك العمق فعلًا؛ وما لا قياس موزونًا له إطلاقًا يعاد `None` ويعلَن بالاسم في `depth_unknown_fields`. تخزين العمق بالأدلة (`_166_live_weight`/`_absorb_units`/`_remember_section_live`) صار `_measured` (قياس أو None — قاعدة أ٨ ممدودة للعمق) كي لا يجرّ صفر ملفق المتوسط.
  - **الحالة المجمعة (حكم المالك ٢٠٢٦-٠٨-٢٠ «ب NQ» — سجل الختم بند 28):** `aggregate_state` بقاعدة دمج حالة المسارين المختومة نفسها (166`._merge_paths` حرفيًا) فوق بطاقات الأقسام الستة 150/200/250/300/350/400: لا حالات ⇒ NOT_READY · أي STALE ⇒ STALE · أقل من الستة أو أي ANALYZING ⇒ ANALYZING · الستة كلها READY ⇒ READY · غير ذلك ⇒ NOT_READY. حالة البطاقة الحقيقية بتوحيد v2.6.7 (`unified.state` حيث توجد، و`section_contract.state` لبطاقة 166 الحية، والمسطح لغيرهما)، والغائب يعلَن بأرقامه في `state_missing_sections`. عائلة تعلن حالة صادّة (STALE/ERROR) تُصدّ نتائجها كما كان **وتدخل حالتها** المجمعة (`_note_family_state`) — STALE يظهر STALE لا غائبًا صامتًا.
  - اختباران جديدان: العمق الموزون فوق المقيس فقط + إعلان المجهول، وقاعدة الحالة بحالاتها الثلاث — 16/16.

## 2.7.0 — 2026-08-20
- **ولادة معرّف القرار — بختم NQ بند 22 حزمة ب (ب١):** حمولة `decision.aggregated.state` تحمل `decision_id` حتميًّا = `"dec:" + cycle_id` (وcycle_id هو `cycle_key` الكامل من `shared/cycle_identity` — حساب|وسيط|رمز|إطار|بداية). نفس الهوية تعيد نفس المعرّف بأي إقلاع فلا يتكرر قرار، والهوية الست (حساب/وسيط/رمز/إطار/بداية/معرّف) كاملة بالمخرج لتمرّ السلسلة بلا فقد (حكم ق٩ §١٧). اختبار جديد يثبت الولادة والحتمية — 14/14.

## 2.6.7 — 2026-08-20
- **صدق القياس — بختم NQ بند 22 حزمة أ (أ٨/أ٩):**
  - `_remember_section_live` كفّ عن تلفيق الأصفار: بطاقة section.live لا تحمل `direction`/`strength`/`score` تُسجَّل حقولها `None` مع `weight_known=False`، ولا يدخل وزنها `available_weight`/المتوسط الموزون إلا حين يكون الاتجاه والقوة مقيسَين فعلًا (دالة `_measured` الجديدة — قياس أو `None`، لا صفر احتياطي).
  - قراءة حالة بطاقة القسم توحّدت على `unified.state` حين تتوفر الكتلة المختومة (`_on_section_live` و`_remember_section_live`) — المسطّح المتناقض لم يعد يفتح القبول؛ البطاقات غير المختومة تبقى على الفحص المسطّح الثلاثي.
  - رأس الحمولة صادق: `signal`/`score`/`confidence` صارت `None` بدل `""`/`0`/`0.0` الملفّقة — 451 لا يحسب هذه الحقول أصلًا، و`weighted_direction`/`weighted_strength` بلا تغيير. تحقُّق المستهلكين: 452 يقرأ `evidence`/`complete`/`cycle_status` فقط و450 يقرأ الهوية فقط — لا كسر.
  - اختبارات: تحديث اختبار «تجاهل unified المضلِّل» إلى الدلالة الجديدة (unified هي الحقيقة)، واختبار جديد للحقول غير المقيسة — 13/13.
- **تنظيف لغة الكود (م3/ملف4):** حذف تعليقين عربيّين من `atom.py` (محاسبة الغياب بالاسم في `_publish_cycle`، ومبدأ الحداثة في `health_check`) — مضمونهما مسجّل أدناه في بند `2.6.5` فلا فقدان معرفة. صفر تغيير في المنطق، ولا نصوص عربية في رسائل هذه الذرّة أصلًا.

> **٢٠٢٦-٠٨-١٩ · `2.6.5` — عين المالك + مبدأ الحداثة (صفر تغيير بحكم التجميع):**
> عدّاد غياب لكل عائلة (`missing_by_family`) والرسالة تعلن `top_missing` — «أي عائلة
> تجوّع الاكتمال» (مقيس: اكتمال 7 من 209 بالإقلاع الأخير) يُقرأ من اللوحة مباشرة.
> والصحّة تحكم على الجديد منذ آخر فحص (`new_late`/`new_invalid`) لا على تراكم العمر —
> نفس إصلاح 617 v3.0.1 و400 v1.2.0. والرسالة تسمّي عدّاد «deadline» باسمه الصادق:
> `superseded` (هو نشرُ دورةٍ داستها دورةٌ أحدث، لا مهلة زمنية).

**الذرّة:** `٤٥١` · **الإصدار:** `2.6.3`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.6.3` | **تحقيق حي كامل ثم إزالة أداة التشخيص (بأمر صريح "احذف الأثر"):** تتبّع مباشر (WebSocket، بلا تعديل) كشف أن ٣ دورات متتالية أعطت `present=['166'], missing=['350','400','401'], reason='superseded'` رغم أن ٤٠٠/٣٥٠/٤٠١ تنشر أدلة كاملة وصحيحة الهوية (`cycle_id` مطابق تماماً) خلال أقل من ثانية من إغلاق كل شمعة — العطل لم يكن عندهم. رُكِّب أثر تشخيصي مؤقت داخل ٤٥١ فقط (حدث جديد `diag.451.trace.temp` لا يشترك فيه أحد، أربع نقاط تسجيل: `_on_candle`/`_cycle_for`/`_absorb_units`/`finalize`) لتحديد أين بالضبط تُفقَد الأدلة — بصفر تغيير في منطق `_cycle_for`/`_absorb_units`/`cycle_key_of`. إعادة التحميل الحي لتركيب الأثر (٤٥١ **بلا** `snapshot()`/`restore()`) صفّرت ذاكرتها الداخلية (`self._cycles`/`self._open`/`self._closed`/`self._published`) بالكامل. بعد التصفير: **٢٧/٢٧ دورة متتالية على مدى ٢٥ دقيقة كاملة أعطت `complete` بلا استثناء واحد** — صفر `_cycle_for` بحالة "غير موجودة"، صفر رفض بـ`_absorb_units`. **الحكم النهائي المتّفق عليه**: لا يوجد دليل كودي يبرر لمس `_cycle_for` أو `_absorb_units` أو `cycle_key_of` — الانقلاب من فشل دائم (٣/٣) إلى نجاح دائم (٢٧/٢٧) تزامن تماماً مع تصفير الذاكرة الداخلية، وهذا يرجّح **حالة داخلية متراكمة (stale state)** لا خللاً منطقياً ثابتاً بالترتيب — لكن هذا **دليل ظرفي غير مباشر**، لم يُمسَك أثر مباشر لدورة فاشلة أثناء التركيب (لم تتكرر الحالة الفاشلة رغم المراقبة الممتدة). بأمر صريح: أُزيلت أداة التشخيص بالكامل (الحدث `diag.451.trace.temp` ونقاط التسجيل الأربع) دون لمس أي منطق قرار — الملف عاد لمطابقة سلوك `2.6.1` الأصلي تماماً (١٢/١٢ اختبار قديم مرّ بلا أي تعديل عليه، والمدقّق البنيوي رجع لمخالفته الوحيدة المسبقة). **العطل الأصلي يبقى غير محسوم نهائياً — مرحّل لملاحظة حالة تراكمية محتملة إن ظهر مجدداً أثناء تشغيل طويل، لا حل مطبَّق.** |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.

## 2.11.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

## 2.12.0 — 2026-08-23
- المسار (أ) من ورقة X (أمر تبنّي المالك ٢٠٢٦-٠٨-٢٣): المرور بالمحوّل القانوني `as_validated_tick` كبقية ذرّات العقد — هوية الدورة تُؤخَذ جاهزة منه بسلسلة الاحتياط (sequence ← tick_id ← event_id ← period_start ← stamp@bid:ask)، فتُفتح الدورة من أي مصدر تِكّات ولو بلا ترقيم، وتتوحّد هوية القرار مع هوية الأقسام. كان العطل: رفض أي تِكّة بلا sequence بينما 613 لا يمرّره — فماتت السلسلة كلها (20 ألف تِكّة وصفر دورة).

## 2.13.0 — 2026-08-23
- البناء ٤ وصلة ٣ (أمر المالك ٢٠٢٦-٠٨-٢٣): طريق واحد — أُضيف `analysis.section.live → عائلة 150` لخريطة بطاقات الأقسام، وأُغلق استهلاك `analysis.raw.completed` نهائيًّا (لو بقيا معًا لتضاعف وزن التحليل في القرار — الإغلاق شرط لا خيار)، وعائلة التحليل الافتراضية صارت 150. دليل التحليل يُختم في الدورة مرة واحدة عبر بطاقة القسم.


## 2.14.0 — 2026-08-23
**إقفال القسم 150 · مرحلة ٢: الروم المتدفّق — حدث جديد `decision.room.state`.**

> من رؤية المالك المسجّلة حرفيًّا (ورقة رؤية المالك ٢٠٢٦-٠٨-٢٣): «هذا المجمع الأرقام دائمًا كل تكية عنده عم بتتغير بسلاسة… ما في حدا مستني الثاني. ما في حدا معتمد على الثاني. ما في شيء بالنهار إذا حدا منهم ما صرّح.»

- **كل بطاقة قسم هويتها كاملة تدخل الروم مهما كانت حالتها** — `NOT_READY` كانت تُرمى؛ الآن تدخل بما قالته بوزنها. (مسار الدورات القديم وجداره الـREADY لم يُمسّ — الروم طبقة إضافة.)
- **أرقام شاملة متدرجة:** اتجاه/قوة/ثقة/جاهزية موزونة بأوزان الأقسام على ما قالوه — تتحرك فقط بسرعة مدخلاتها، بلا قفز ولا تصفير. الثقة غير المقيسة `null` معلنة `confidence_defined:false`.
- **صف لكل قسم داخل الروم:** آخر ما قاله · حالته · وزنه · جاهزيته٪ · عمر بطاقته `age_s` — والأقسام الغائبة معلنة بالاسم في `sections_missing`.
- صحّة ظاهرة: `room_sections` · `room_updates` · `room_emitted`.
- الاختبار الجديد: بطاقة NOT_READY تدخل، الرقم يتحرك 40→7 بتدرج بوزنين مختلفين، وبلا هوية يُرفض.
- **ملاحظة أمانة:** هذا القياس نُسخ في 2.16.0: شُطر الروم والإخراج والصحّة وصار atom.py تحت حدّ 350.

## 2.16.0 — 2026-08-24 — Patch قبل الختم
- تطبيق سياسة latest-same-scope عند تعطيل تطابق الدورة، وفصل الإخراج والصحّة والروم، وإكمال عقد الروم بثمانية حقول بينها ratio، مع direction=null للمجهول.

- 2026-08-25 · v3.0.0 — the batch dies, the room decides: latest delivery per source held with its age; per-family freshness horizons (owner STALE_AFTER_S dial + family_freshness_s config); presence != readiness (every fresh section card enters the room, only READY weight contributes); strategy.cycle.collected re-subscribed (the ONLY directional evidence source had been disconnected); superseded machinery retired (was: 204,236 cycles, complete=0). (nq seal, owner vision: nobody waits for anybody)

- 2026-08-25 · v3.0.1 — numeric strategy directions (+-100 contract) are parsed as buy/sell/neutral; the word-table alone read every tick-native unit as UNKNOWN so all evidence landed kind=context and 452 had zero eligible rows (measured live: 31/31 CONTEXT_ONLY). (nq seal)

# التاريخ — 455_قرار_الشراء

**الذرّة:** `٤٥٥` · **الإصدار:** `1.3.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٠ | `1.3.0` | **بختم NQ بند 22 حزمة ب — توصيل الثمانية.** القياس الحي الدافع: 276 دورة كلها `FIELD_UNKNOWN:direction` لأن 453 يبث الاتجاه كلمة والفاحص يقرؤها رقمًا. صار يقرأ مفاتيح 453 v3.8.0 الفعلية: القيمة الاتجاهية من `direction_value` (رقم موقّع ±100)، والقوة من `strength_value`، والثقة من `confidence_value` (مقياسا ×100)، والعمق من `current_depth` (مجمع 451 الموزون)؛ وفحص READY يقرأ الحالة المجمعة `aggregate_state` (حكم المالك ٢٠٢٦-٠٨-٢٠ «ب NQ» — سجل الختم بند 28: قاعدة دمج حالة المسارين فوق الأقسام الستة عند 451). أسماء الفحوص وأسباب `FIELD_UNKNOWN` بقيت بأسماء العقد، والإعلان بـ`unknown_fields` يعلو القيمة تحت الاسمين (المصدر واسم العقد)، والغائب فعلًا يبقى `FIELD_UNKNOWN:اسم` كما كان. اختبار جديد بحمولة 453 الواقعية: مثال ق٨ §١٦ (+72/61/78/81) يصير مؤهلًا فعليًا مع `aggregate_state=READY` ويسقط `FIELD_UNKNOWN:state` وحده حين لا حالة — 9/9. |
| ٢٠٢٦-٠٨-٢٠ | `1.2.0` | **بختم NQ بند 22 حزمة ب — ق٨/ق٦: فاحص أهلية الشراء داخل القرار الواحد.** لم يعد «قرار شراء» موازيًا: يستلم دورة القرار نفسها من 453 (`decision.scored.state`) وينشر حالة أهلية `decision.eligibility.buy.state` بالهوية الست (حساب/وسيط/رمز/إطار/بداية دورة/معرّف قرار) + `status` من {eligible/not_eligible} + `reason` إنكليزي أو None + قائمة `checks` (اسم/قيمة/عتبة/passed) + `source_timestamp`. عتبات ق٦ الحرفية عياراتٍ محكومة: `DECISION_BUY_MIN_DIRECTION=50.0` (ملكه) و`DECISION_MIN_STRENGTH=45.0` و`DECISION_ELIGIBILITY_MIN_CONFIDENCE=63.0` و`DECISION_MIN_CURRENT_DEPTH=45.0` (ملك 457، تُقرأ بـ`effective_value` كل دورة) والحالة `READY` شرط بنيوي لا عيار — والنسبة بلا معنى أو عتبة مخترعة (نص الحكم). المجهول (`unknown_fields`/غائب) يحجب بسبب معلَن `FIELD_UNKNOWN:اسم_الحقل` ولا يُقرأ صفرًا؛ وغياب `decision_id` بالنشر المتدرج يُعلَن `identity_incomplete` ولا يُخترع. لا يعيد تحليلًا، لا يغيّر قيمة، لا يتجاوز فلترًا، لا يحسم تعارضًا (458)، لا ينشئ أمرًا. `DECISION_ELIGIBILITY_MIN_CONFIDENCE` مستقل عن `DECISION_MIN_CONFIDENCE` القائم (452، مقياس 0-1 لثقة الدليل) — معنيان مختلفان فلا دمج. اختبارات: أمثلة ق٨ §١٦/§١٨/§١٩ حرفيًا + المجهول يحجب + الهوية تمرّ + عيار حي (8/8). |
| ٢٠٢٦-٠٨-١٨ | `1.1.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

## 1.4.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

# Risk Read-Only Aggregator

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٠٠` |
| الاسم | Risk Read-Only Aggregator |
| المجلّد | `500_مدير_المخاطر` |
| الإصدار | `2.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `risk.account.state`
- `risk.exposure.state`
- `risk.profit_limits.state`
- `risk.session_limits.state`

**تُرسِل (من `manifest.yaml`):**
- `risk.unified.state`

<!-- BITAQA_END -->

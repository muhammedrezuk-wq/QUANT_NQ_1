# التاريخ — 452_تقييم_الإشارات

## 2.4.0 — 2026-08-20
- **بختم NQ بند 22 حزمة ب — توصيل الثمانية (تمرير):** الحقول المجمعة المقاسة عند 451 تمرّ من المدخل للمخرج كما هي إن وُجدت — `current_depth`/`required_depth`/`depth_unknown_fields` + `aggregate_state`/`state_missing_sections` (الأخيران بحكم المالك ٢٠٢٦-٠٨-٢٠ «ب NQ» — سجل الختم بند 28). الحاضر يمرّ بقيمته (وNone يبقى None معلَنًا)، والغائب لا تُخترع مفاتيحه. اختبار جديد — 8/8.

## 2.3.0 — 2026-08-20
- **الهوية الست بلا فقد + إزالة الأصفار الثابتة — بختم NQ بند 22 حزمة ب (ب١/ب٦):**
  - كان الوسيط (`broker`) يسقط هنا بالذات (مسح موثق): الآن الهوية الست (حساب/وسيط/رمز/إطار/بداية/معرّف) تقرأ من المدخل وتعاد بالمخرج كاملة (حكم ق٩ §١٧)، والغائب يعاد `None` — لا يكمَّل بالاختراع — مع إنذار `identity_incomplete` وقائمة `identity_missing` بأسماء الغائب.
  - الأصفار الثابتة عند سطر ~157 (`""/0/0.0` كانت تنشر كقياس) استبدلت بالإعلان الصادق `None` بنمط 451 v2.6.7 — 452 لا يحسب `signal`/`score`/`confidence` أصلًا. المستهلك الوحيد (453) يقرأ `evidence`/`complete` فقط — لا كسر.
  - اختبارات: هوية كاملة/ناقصة + رأس None — 7/7.

## 2.2.1 — 2026-08-20
- **تنظيف لغة الكود (م3/ملف4 — عربي داخل atom.py):** حذف تعليق عربي واحد من `initialize` (عيارات المالك من سجلّ المُعامِلات تعلو المانيفست — مضمونه مسجّل في بند `2.2.0` أدناه). صفر تغيير في المنطق، لا نصوص عربية في رسائل الذرّة.

**الذرّة:** `٤٥٢` · **الإصدار:** `2.2.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.2.0` | **عيارا المالك من اللوحة:** `min_confidence` و`low_quality_factor` مُعامِلان محكومان بسجلّ المُعامِلات ([[shared/decision_dials.py]]) — قيمة المالك المعتمدة تعلو المانيفست، التعديل عبر ٩٠١ بتأكيد اللوحة بخطوتين، تطبيق حي + نشر `decision.settings.state` للّوحة. |
| ٢٠٢٦-٠٨-١٨ | `2.1.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

## 2.4.1 — 2026-08-23
- البناء ٣ من ورقة X: كل مدخل يُرمى يُحصى برمز سببه ويظهر في `health_check` (dropped · drop_reasons) — لا سقوط صامت بعد اليوم. أول رمز: IDENTITY_MISSING في معالج `_on_aggregated`. لا تغيير في منطق التقييم أو الدرجة.

# التاريخ — 456_قرار_البيع

**الذرّة:** `٤٥٦` · **الإصدار:** `1.3.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٠ | `1.3.0` | **بختم NQ بند 22 حزمة ب — توصيل الثمانية.** كما 455 v1.3.0 وبنفس القياس الدافع (الكلمة كانت تُقرأ رقمًا فتحجب كل الدورات بـ`FIELD_UNKNOWN:direction`): القيمة الاتجاهية من `direction_value` (والعيار الموجب يطبَّق على الجانب السالب كما هو: ≤ −العيار)، والقوة من `strength_value`، والثقة من `confidence_value`، والعمق من `current_depth`، وفحص READY من الحالة المجمعة `aggregate_state` (حكم المالك ٢٠٢٦-٠٨-٢٠ «ب NQ» — سجل الختم بند 28). أسماء الفحوص وأسباب `FIELD_UNKNOWN` بأسماء العقد، والإعلان يعلو القيمة تحت الاسمين، والغائب يبقى معلَنًا لا مخترَعًا. ملاحظة صيانة: ملف اختبارات 1.2.0 بُتر بحادث أداة أثناء هذه الجلسة وأعيد بناؤه نصًّا من قراءته الكاملة الموثقة بنفس الجلسة (الاختبارات التسعة نفسها بأصولها وتوكيداتها) ثم حُدّث مساعده للمفاتيح الجديدة — 9/9. |
| ٢٠٢٦-٠٨-٢٠ | `1.2.0` | **بختم NQ بند 22 حزمة ب — ق٨/ق٦: فاحص أهلية البيع داخل القرار الواحد.** لم يعد «قرار بيع» موازيًا: يستلم دورة القرار نفسها من 453 (`decision.scored.state`) وينشر حالة أهلية `decision.eligibility.sell.state` بالهوية الست + `status` من {eligible/not_eligible} + `reason` إنكليزي أو None + قائمة `checks` (اسم/قيمة/عتبة/passed) + `source_timestamp`. عتبات ق٦ الحرفية: `DECISION_SELL_MIN_DIRECTION=50.0` (ملكه؛ العيار موجب ويُطبَّق على الجانب السالب: القيمة الاتجاهية ≤ -50.0000) + عيارات 457 المشتركة (قوة 45 · ثقة 63 بمقياس 0-100 · عمق 45 تُقرأ بـ`effective_value` كل دورة) والحالة `READY` شرط بنيوي — والنسبة بلا معنى أو عتبة مخترعة (نص الحكم). المجهول يحجب بسبب معلَن `FIELD_UNKNOWN:اسم_الحقل` ولا يُقرأ صفرًا؛ وغياب `decision_id` يُعلَن `identity_incomplete` ولا يُخترع. لا يعيد تحليلًا، لا يغيّر قيمة، لا يتجاوز فلترًا، لا يحسم تعارضًا (458)، لا ينشئ أمرًا. اختبارات: أمثلة ق٨ §١٧/§١٦/§١٨/§١٩ حرفيًا + المجهول يحجب + الهوية تمرّ + عيار حي (9/9). |
| ٢٠٢٦-٠٨-١٨ | `1.1.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

## 1.4.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

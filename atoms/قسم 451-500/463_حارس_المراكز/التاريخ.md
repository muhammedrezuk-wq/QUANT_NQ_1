# التاريخ — 463_حارس_المراكز

## 1.2.1 — 2026-08-20
- **تنظيف لغة الكود (م3/ملف4 — عربي داخل atom.py):** حذف ٦ مواضع تعليقات عربية من `atom.py` (عيار المالك، الحكم عند «المسجّل»، اشتقاق `by_symbol`، إنعاش الأحكام مع كل صورة مراكز، حكم عند الطلب، لا حكم قبل أول صورة) — مضمونها كله مسجّل في بنود `1.2.0`/`1.1.2`/`1.1.1`/`1.1.0` أدناه. صفر تغيير في المنطق.

**الذرّة:** `٤٦٣` · **الإصدار:** `1.2.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `1.2.0` | **عيار المالك من اللوحة:** `max_per_symbol` مُعامِل محكوم بسجلّ المُعامِلات ([[shared/decision_dials.py]]) — قيمة المالك المعتمدة تعلو المانيفست، التعديل عبر ٩٠١ بتأكيد اللوحة، تطبيق حي + نشر `decision.settings.state`. (مسار `risk.asset.command SET_MAX_PER_SYMBOL` القديم باقٍ كما هو.) |
| ٢٠٢٦-٠٨-١٩ | `1.1.2` | **إنعاش الأحكام مع كل صورة مراكز:** حتى مع التبكير، ترتيب التوصيل بالناقل (بأرقام الذرّات) يجعل حكم الدورة يصل بعد تقييم 454 لنفسها، ودورة القرار أطول من مهلة النضارة → `stale=position_filter` مقيسة حيًّا رغم كل شيء. الحل بلا أي رقم مخترَع: مع **كل** صورة مراكز من 609 (~كل ثانية) تُعاد أحكام كل الرموز التي سبق السؤال عنها — عمر الحكم ثانية دائمًا، وإيقاعه إيقاع الحقيقة نفسها. |
| ٢٠٢٦-٠٨-١٩ | `1.1.1` | **تبكير الحكم خطوة:** الحكم عند «المحسوم» كان يصل بعد تقييم 454 لنفس الحدث دومًا (ترتيب الاشتراك بالناقل) فيُقرأ حكم الدورة السابقة وقد بات (`stale=position_filter` مقيسة حيًّا). صار الحكم يصدر عند «المسجّل» (`decision.scored.state` — خطوة قبل المحسوم عبر 458) **وأيضًا** عند المحسوم إنعاشًا — فيكون طازجًا لحظة التقييم دائمًا. |
| ٢٠٢٦-٠٨-١٩ | `1.1.0` | **فكّ قفل «صفر صفقات» — عطل حي مقيس:** الذرّة كانت (أ) ترمي كل رسائل 609 لأنها تشترط حقل `by_symbol` الذي لا ينشره (بقيت `NO_INPUT_YET` أبديًّا)، و(ب) بتصميم «الصمت = مرور» تسكت عن أي رمز بلا مراكز — بينما 454 يفشل مغلقًا: «الصمت = حظر». النتيجة المقاسة: 454 `passed=0/224` وكل القرارات `BLOCKED_UPSTREAM` محايدة صفرية. الإصلاح: (١) تسامح مع عقد 609 الفعلي (اشتقاق `by_symbol` من قائمة المراكز)، (٢) **حكم عند الطلب**: مع كل `decision.resolved.state` يصدر حكم مرور/حظر صريح طازج للرمز من عدّاد المراكز الحالي (متعدد الحسابات) — لا صمت بعد اليوم، ولا حكم يُخترع قبل أول صورة مراكز حقيقية. ٤ حرّاس جدد → ٩/٩. |
| ٢٠٢٦-٠٨-١٨ | `1.0.1` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

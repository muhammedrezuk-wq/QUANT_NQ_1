# التاريخ — 457_قرار_الانتظار

**الذرّة:** `٤٥٧` · **الإصدار:** `1.3.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٠ | `1.3.0` | **بختم NQ بند 22 حزمة ب — توصيل الثمانية.** كما 455/456 v1.3.0: القيمة الاتجاهية من `direction_value` (للجانبين معًا)، والقوة من `strength_value`، والثقة من `confidence_value`، والعمق من `current_depth`، وفحص READY من الحالة المجمعة `aggregate_state` (حكم المالك ٢٠٢٦-٠٨-٢٠ «ب NQ» — سجل الختم بند 28) — فتفسير الانتظار وحقول الحاجب (`blocking_field`/`blocking_value`/`blocking_threshold`) صارت تقرأ الأرقام الفعلية بدل حجب دائم بـ`FIELD_UNKNOWN:direction`. أسماء الفحوص والأسباب بأسماء العقد، والغائب يبقى معلَنًا لا مخترَعًا — 10/10. |
| ٢٠٢٦-٠٨-٢٠ | `1.2.0` | **بختم NQ بند 22 حزمة ب — ق٨/ق٦: تثبيت وتفسير الانتظار وسببه داخل القرار الواحد.** يستلم دورة القرار نفسها من 453 (`decision.scored.state`) وينشر `decision.wait.state` بالهوية الست: `status="inactive"` حين يوجد جانب مؤهل (والسبب `BUY_SIDE_ELIGIBLE`/`SELL_SIDE_ELIGIBLE`/`BOTH_SIDES_ELIGIBLE` — وعند تأهل الجانبين لا يحسم: الحسم عند 458)، و`status="eligible"` = الانتظار فعّال مع السبب الأول المعروف (`DIRECTION_INSUFFICIENT`/`STRENGTH_BELOW_THRESHOLD`/`CONFIDENCE_BELOW_THRESHOLD`/`DEPTH_BELOW_THRESHOLD`/`STATE_NOT_READY`/`FIELD_UNKNOWN:…`) + `blocking_field`/`blocking_value`/`blocking_threshold` حين تُعرف — ولا يحوّل انتظارًا لجانب أبدًا. يملك عيارات ق٦ المشتركة الثلاثة: `DECISION_MIN_STRENGTH=45.0` و`DECISION_ELIGIBILITY_MIN_CONFIDENCE=63.0` و`DECISION_MIN_CURRENT_DEPTH=45.0` (هو الفاحص المستهلك لها للجانبين معًا لتفسير الانتظار)، ويقرأ عياري الاتجاه (ملك 455/456) بـ`effective_value` كل دورة. الحالة `READY` شرط بنيوي والنسبة بلا عتبة مخترعة. المجهول يفعّل الانتظار بسبب معلَن لا صفرًا؛ وغياب `decision_id` يُعلَن `identity_incomplete` ولا يُخترع. اختبارات: أمثلة ق٨ §١٦/§١٧/§١٨/§١٩ حرفيًا + المجهول + الهوية + عيار حي (10/10). |
| ٢٠٢٦-٠٨-١٨ | `1.1.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

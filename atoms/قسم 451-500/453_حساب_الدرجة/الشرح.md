# Score Calculator

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٤٥٣` |
| الاسم | Score Calculator |
| المجلّد | `453_حساب_الدرجة` |
| الإصدار | `3.10.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `decision.evaluated.state`
- `decision.settings.command`

**تُرسِل (من `manifest.yaml`):**
- `decision.scored.state`
- `decision.settings.state`

**إعداداتها:** `directional_weight` · `context_weight` · `min_participation` · `directional_sources`

<!-- BITAQA_END -->

# التاريخ — 467_إرسال_القرار

**الذرّة:** `٤٦٧` · **الإصدار:** `2.1.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٨ | `2.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |
| ٢٠٢٦-٠٨-٢٠ | `2.1.0` | **بختم NQ بند 22 حزمة ب — ق٩ (ب٤):** إلغاء وسم التشخيص — 467 صار بوابة الانتقال الوحيدة من القرار المعتمد إلى التنفيذ. ينشر ثلاثة أحداث بوابة: `decision.gate.passed` (معتمد + جانب buy/sell — حمولة تمرير كامل تحفظ الهوية الست والثمانية والحواجز كما وصلت، + `decision_side` + `gate_request_id` حتمي = معرّف القرار + `:req1` + `gated_at` من نبضة `SYS_SECOND`) · `decision.gate.blocked` (approved=false لا يمر أبدًا — مع `reject_reason` والهوية كما وصلت) · `decision.gate.recorded` (انتظار = حالة قرار لا أمر). منع التكرار: نفس معرّف القرار لا ينشر passed مرتين (ذاكرة داخليّة تنجو من الإقلاع عبر اللقطة، سقفها `4096` معرّفًا يُطرد أقدمها — رقم سعة ذاكرة لا عيار تداول)؛ التكرار الموثّق بحقل `redispatch_reason` يرفع اللاحقة (`:req2`…). نشر متدرّج: حمولة 466 القائمة اليوم بلا الهوية الست تمرّ بالمتوفّر مع إعلان `identity_incomplete` (المعرّف المتاح فعلًا هو `cycle_id` فيُبنى منه معرّف الطلب — لا اختراع لهوية غائبة)؛ قرار بلا أيّ معرّف يُحجب بـ`NO_DECISION_IDENTITY` لأن حكم ق٩ يوجب معرّف قرار + معرّف طلب على كلّ محاولة. سجلّ الإرسال `decision.dispatch.state` باقٍ لمخزن 707 (حكم البند 61 «لا إعلان يُحذف») لكنه صار `executable: true` بلا ملاحظة تشخيص، ويحمل `gate_request_id` و`decision_id`. عدّادات الصحّة: seen/passed/blocked/recorded/duplicates/identity_incomplete. المرحلة/القيمة/العتبة عند الحجب تسافر داخل `barriers` كما وصلت — لا تُصنَّع. المانيفست: `metadata.executable: true` (خانة الجذر يرفضها عقد المانيفست الصارم) و`startup_mode: auto` كما كان |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

- 2026-08-25 · v2.2.0 — identity basis aligned (dashboard-model finding, nq seal): IDENTITY_FIELDS uses period_start like 452/453/454/466 (was cycle_id, the lone deviator) so identity_incomplete is computed on ONE basis across the chain. cycle_id stays in the payload as information.

## 2026-08-27 — إصلاح اختبار قديم (لا تغيير بـatom.py)

`tests/test_atom.py::test_q9_1_approved_passes_with_full_identity` كان يفترض «هوية كاملة» بلا
`period_start` — صحيح **قبل** v2.2.0 أعلاه (يومها كانت `cycle_id` وحدها الأساس)، لكن v2.2.0 غيّر
`IDENTITY_FIELDS` ليطابق 452/453/454/466 (`period_start` بدل `cycle_id`)، ولم تُحدَّث بذرة `_contract()`
حينها. أُضيف `"period_start": 1000.0` لصانع العقد — يطابق ما تنتجه 466 فعلاً اليوم. 10/10 خضراء،
0 مخالفات.

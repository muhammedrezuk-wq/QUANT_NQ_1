# Asset Control Filter

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٤٦٨` |
| الاسم | Asset Control Filter |
| المجلّد | `468_التحكم_بالأصول` |
| الإصدار | `1.5.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.tick.validated`
- `perpetual.asset.activate`
- `risk.asset.command`

**تُرسِل (من `manifest.yaml`):**
- `decision.filter.asset.state`
- `allowed.symbols.state`

**إعداداتها:** `allowed_symbols`

<!-- BITAQA_END -->

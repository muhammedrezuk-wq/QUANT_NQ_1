# Freshness Filter

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٤٦٤` |
| الاسم | Freshness Filter |
| المجلّد | `464_فلتر_الطزاجة` |
| الإصدار | `1.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_SECOND`
- `market.tick.validated`

**تُرسِل (من `manifest.yaml`):**
- `decision.filter.freshness.state`

**إعداداتها:** `db_path` · `table_name` · `poll_interval_s` · `stale_threshold_s`

<!-- BITAQA_END -->

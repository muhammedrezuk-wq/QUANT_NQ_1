# التاريخ — 458_حل_التعارض

## 3.1.0 — 2026-08-20
- **بختم NQ بند 22 حزمة ب — توصيل الثمانية: الحسم مقاد بالأحكام لا بالسباق.** القياس الحي الدافع: آخر حسم `org=fallback` رغم اشتغال الفاحصين — التنازل التعاوني الواحد (`sleep(0)`) كان يخسر سباق التوزيع المتوازي (gather) أحيانًا.
  - **النمط المقاد بالأحداث (تسلسل الورقة الحرفي 453←الفاحصون←458):** عند scored بمعرّف قرار: تُركن مادته (`_pending` بمفتاح decision_id + فهرس نطاق account,broker,symbol,timeframe) **ولا يحسم فورًا** إلا إن كان ثلاثي الأحكام (455 شراء + 456 بيع + 457 انتظار) لنفس المعرّف واصلًا مسبقًا؛ وعند كل حكم أهلية: يخزن، وإن اكتمل الثلاثي والمادة راكنة ⇒ يحسم الحكمُ المكمل (origin=eligibility أو conflict_rule كما كان). scored بلا معرّف قرار يحسم فورًا بالمنطق المعلن كما كان.
  - **دفق الأقدم بالأحدث (نمط 150 المختوم «newer_tick» — الخلف هو المهلة، لا مؤقت ولا رقم مخترع):** scored لدورة أحدث لنفس النطاق والمادة السابقة غير محسومة ⇒ تحسم فورًا بجسم الحسم المعلن نفسه (ثلاثيها لم يكتمل ⇒ fallback + إنذار `eligibility_missing`)، ثم يفتح الأحدث. سعة 256 القائمة بقيت للمخزنين، وفيض الركن يحسم الأقدم بالجسم نفسه — لا إسقاط صامت: كل scored يحسم مرة واحدة بالضبط.
  - أزيل `sleep(0)` (لم يعد لازمًا). الصحة تعلن `pending_scored` و`flushed_stale`. اختبار الناقل الحقيقي حُدّث (الحسم عند اكتمال الثلاثي — أُضيف حكم 457 الذي كان غائبًا عنه) + اختبار جديد للدفق بالأحدث (الثنائي لا يكفي، والخلف يدفق fallback معلنًا) — 13/13.

## 3.0.0 — 2026-08-20
- **استقبال الأهلية وحسم جانب القرار — بختم NQ بند 22 حزمة ب (ب١/ب٣/ب٦):**
  - اشتراكات جديدة (معلنة بالمانيفست): `decision.eligibility.buy.state` (455) · `decision.eligibility.sell.state` (456) · `decision.wait.state` (457) — عقد متفق: هوية ست + `decision_id` + `status` من {eligible, not_eligible, inactive} + `reason` + `checks`. يخزن أحدث حكم لكل `decision_id` (سعة 256 بإزاحة الأقدم).
  - الحسم (حكم ق٨ §٩–١٠ وأمثلة §١٦–١٩): كلاهما مؤهل → **قواعد التعارض القائمة عند 458 تحسم وحدها** (455/456 لا يحسمان)؛ أحدهما فقط → جانب القرار له؛ لا أحد → انتظار. غياب أحكام الأهلية عن معرّف (نشر متدرج) = لا اختراع: المنطق القائم يكمل مع إنذار `eligibility_missing`.
  - المخرج يحمل `decision_side` من {buy, sell, wait} حصرًا (ب٦ — منفصل عن حقول الكلمات القديمة `signal`/`direction` التي بقيت كما هي لمستهلكي اليوم 360/463/581) + سجل الحسم `resolution` (المنشأ: eligibility/conflict_rule/fallback + حالات الثلاثة + سبب 457 + غائب الأهلية).
  - **الهوية (ب١ — حكم ق٩ §١٧):** كان يسقط الحساب والوسيط وبداية الدورة ويخترع `decision_id = cycle_id` محليًّا (مسح موثق) — الآن الست تمرّ من المدخل كما هي، والغائب `None` مع `identity_incomplete` بالأسماء. **كسر دلالة مقصود:** `decision_id` لم يعد يُخترع — يولد عند 451 ويمرّ.
  - تنازل تعاوني واحد (`await asyncio.sleep(0)`) عند رأس الحسم: الناقل يوزّع مشتركي `decision.scored.state` بالتوازي (gather — نفس ميكانيكا 451 الموثقة) وأحكام 455/456/457 تُصفّ خلف مهمة الحسم بنفس الجولة؛ التنازل الصفري (بلا مؤقّت ولا رقم ولا خلفية) يترك المصفوف يهبط أولًا، ومع غيابها الفعلي يبقى fallback المعلن. مثبت باختبار على `core.event_bus` الحقيقي.
  - الصحّة تعلن الجوانب والمناشئ وعدادات الأهلية. اختبارات: الحالات الأربع + fallback + الهوية + الناقل الحي — 12/12.

## 2.1.1 — 2026-08-20
- **تنظيف لغة الكود (م3/ملف4 — عربي داخل atom.py):** حذف ٣ تعليقات عربية من `atom.py` (عيارات المالك — بند `2.1.0`؛ أرقام آخر حلّ بالصحّة — بند `2.0.1`). سببٌ كان موثّقًا بالتعليق فقط ويُحفظ هنا: **نشر حالة العيارات عند الإقلاع** (`start` تستدعي `_publish_dials_state`) كي يأخذ المشترك المتأخّر (اللوحة، عبر إعادة تشغيل ٠٣) آخر حالة فورًا فلا تفتح صفحة الإعدادات فارغة. صفر تغيير في المنطق.

**الذرّة:** `٤٥٨` · **الإصدار:** `2.1.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.1.0` | **عيارا المالك من اللوحة** (أمره المباشر: «العتبات بالآخر عيارهم بإيدي من اللوحات، بدقّة 100.00»): `neutral_band` و`conflict_ratio` صارا مُعامِلَين محكومَين في سجلّ المُعامِلات ([[shared/decision_dials.py]]) — القيمة المعتمدة من المالك (مصدر OWNER + نسخة + سجلّ تدقيق، تعيش بعد إعادة التشغيل) تعلو قيمة المانيفست؛ التعديل حصرًا عبر بوّابة ٩٠١ (`decision.settings.command` بتأكيد اللوحة بخطوتين)، والتطبيق حي فوري مع نشر الحالة `decision.settings.state` للّوحة، والقيم السارية معلنة بتفاصيل الصحّة. |
| ٢٠٢٦-٠٨-١٩ | `2.0.1` | **رؤية عيارة الحياد (صفر تغيير بالحكم):** أرقام آخر حلّ تُعلن في رسالة الصحّة — النسبة الفعلية `|net|/available` مقابل `neutral_band` والاتجاه الناتج. مقيس حيًّا: إشارات شراء بدرجة ١٠٠ ومشاركة ٥٢٪ من 453 كانت تُحيَّد هنا كلها (`buy=0 neutral=5`) لأن الصافي منسوبٌ لكامل الوزن المتاح (بمن فيهم الصامتون) فيغرق تحت `0.05`. العتبة وأساس النسبة قرارُ مالكٍ لا كود — الرقم الآن ظاهر ليُقرَّر على بيّنة. |
| ٢٠٢٦-٠٨-١٨ | `2.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

## 2026-08-27 — إصلاح اختبار قديم (لا تغيير بـatom.py)

`test_live_bus_eligibility_lands_before_settlement` يستخدم `core.event_bus.EventBus` الحقيقي
لا وهميّاً، ويقرأ `resolved` **فور** `await bus.publish(...)` — صحيح يوم كُتب (٣.١.٠، ٢٠٢٦-٠٨-٢٠)،
لكن إعادة تصميم الناقل لاحقاً (V3.0، صناديق بريد لكل مستهلك) جعلت `publish` تودع فقط وتعود؛
التسليم الفعلي يقع بمهام خلفية. القياس الصحيح بعد التسليم لا بعد الإيداع — أضيف
`assert await bus.drain(timeout_s=5.0)` بعد النشر مباشرة، مطابقاً اصطلاح الناقل الحقيقي نفسه
(`core/event_bus.py:drain` وتوثيقه). صفر تغيير بـatom.py — منطق الحسم صحيح أصلاً، أثبتّه بإعادة
إنتاج نفس السيناريو يدوياً: `resolved` فارغة فور النشر، وتمتلئ بحسم واحد صحيح فور `drain`.
13/13 خضراء، 0 مخالفات.

# التاريخ — 007_سلامة_الملفات

**الذرّة:** `٧` · **الإصدار:** `4.2.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٥ | `4.2.0` | **مفتاح المسار وُحّد على POSIX — بختم `NQ`:** `_walk_source` كان يبني المفتاح بـ`os.path.relpath` وحده، فيُخرج الفاصل الخلفيّ على ويندوز (`001_NQ\atom.py`) بينما `integrity_baseline.json` مبنيّ بالفاصل الأماميّ (`001_NQ/atom.py`). **المقيس:** الخطّ القاعديّ **١٬٢١١ مفتاحًا · ١٬١٢٢ منها بشرطة أماميّة · وصفر بشرطة خلفيّة** ⇒ **لا مفتاح يتطابق**، فقُرئ كل ملفّ «مضافًا» (`+2233 more`) وسقط الحارس `UNTRUSTED` وأطلق إنذارًا حرجًا. صار المفتاح يُطبَّع بـ`.replace(os.sep, "/")` فيصير الخطّ القاعديّ محمولًا بين النظامين. **صفر تغيير آخر:** البصمة والمسح والنطاق واللقطة كما هي. |
| — | `4.1.0` | **غير مسجَّل** — الإصدار كان بالكود والمانيفست ولا سطر له هنا. يُعلَن ولا يُخترَع له وصف. |
| ٢٠٢٦-٠٨-١٨ | `4.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

- **حملة ١–٤٤٩ · الدفعة ب (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 7500` في metadata — مشتق من إيقاع اشتراكات الذرّة، رقم كريم مؤقّت قابل لضبط المالك من اللوحة لاحقًا. صفر تغيير في الكود والسلوك.

## 4.1.0 — 2026-08-24 — Patch قبل الختم
- إضافة baseline إصدار ثابت يُحمَّل ويُفحَص عند start؛ لا تأسيس تلقائي من ملفات التشغيل.

- 2026-08-25 · v4.2.1 — baseline regenerated from THIS sealed tree using the atom's own _collect (the 08-24 patch baseline was built from the models' tree; after the nq surgery the guard trusts the committed state). Version bumped to hot-reload the new baseline. (nq seal)

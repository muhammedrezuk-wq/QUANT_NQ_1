# File Integrity Monitor

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٧` |
| الاسم | File Integrity Monitor |
| المجلّد | `007_سلامة_الملفات` |
| الإصدار | `4.2.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_HOUR`

**تُرسِل (من `manifest.yaml`):**
- `tools.integrity.violation`

**إعداداتها:** `watched_files` · `watched_dirs` · `watched_extensions` · `watched_names` · `min_watched_items` · `baseline_file` · `ignored_dir_names` · `ignored_suffixes`

<!-- BITAQA_END -->

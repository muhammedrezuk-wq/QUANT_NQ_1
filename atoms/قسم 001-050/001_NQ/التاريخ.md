# التاريخ — 001_NQ

**الذرّة:** `١` · **الإصدار:** `1.2.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `1.2.0` | **العيون — بإذن المالك الصريح هذه المرّة** («بتفتح النواة وبتصحح أخطاء وبتختم بعد ما ضيف رقم ١» + كرت أخضر): النواة فُتحت وخُتمت `1.14.0` بحقل اختياري `AtomContext.subscribe_all` (متوافق خلفيًا — افتراضي None)، والذرّة تشترك به على **كل** حدث بالمشروع بلا استثناء وتعدّ: `events_seen_total` (بلا سقف) · `distinct_event_names` (سقف ١٠٠٠ ضد نموّ ذاكرة بلا حدّ، مع علم `capped`) · `last_event_name` — كلها ظاهرة بتفاصيل الصحّة. رشاقة على نواة أقدم: الحقل غائب → مراقبة الختم تبقى كاملة و`eyes_active=false` معلنة بصدق بلا فشل (مثبتة بحارس). اللقطة/الاستعادة تحمل عدّادات العيون وتتسامح مع لقطات `1.1.0`. ٣ حرّاس جدد → **١٧/١٧**. |
| ٢٠٢٦-٠٨-١٩ | `1.0.0` | إنشاء الذرّة — أول قدرة من رؤية NQ: مراقبة `core/CORE.lock` والتنبيه الفوري عند أي تغيّر، بلا لمس core إطلاقاً. رقمها كان `0` بالتصميم الأول، رُفض من عقد المانيفست نفسه (`id >= 1`، المادة ٦) — صُحِّح لـ`1` بأمر المالك، لا لمس core. |
| ٢٠٢٦-٠٨-١٩ | `1.1.0` | تدقيق كامل (٣ زوايا مستقلّة + تحقّق تخاصمي لكل ملاحظة، ١٧ مؤكَّدة من ١٩): (أ) المقارنة كانت على البصمة فقط — تغيّر رقم النسخة أو وقت الختم بلا تغيّر البصمة كان يمرّ بصمت؛ صار يقارن الثلاثة معاً. (ب) تأسيس خطّ الأساس (أول نبضة، أو أول نبضة بعد إعادة تشغيل بلا لقطة) كان صامتاً تماماً؛ صار يُعلَن بحدث `core.integrity.alert` بعلامة `baseline_established`. (ج) `restore()` كانت صارمة على البصمة فقط وتتساهل بصمت مع باقي الحقول (تحويل نوع أو تصفير عدّاد دون رفض)؛ صارت صارمة على الحقول الخمسة كلّها. (د) رسالة الحالة كانت تقول "core_version تغيّر" حتى لو التغيّر بالبصمة فقط؛ صارت تذكر الحقول الحقيقية المتغيّرة. (هـ) ٨ اختبارات حارس جديدة (JSON تالف/بلا root_digest/غير كائن، health_check وsnapshot قبل أي نبضة، رفض حالات restore الفاسدة، تسلسل تغييرات متعددة). (و) توثيق: عنوان الشرح كان لسا "٠٠٠"، بطاقة BITAQA مفقودة، رقم الذرّة بالتاريخ بالإنكليزي لا العربي، مفتاح وحدة الاختبار الديناميكي كان `_atom000` — الكل صُحِّح. مصدر التدقيق: ورشة مراجعة مستقلّة بثلاث زوايا (صحّة/اتساق/اكتمال اختبارات) + تحقّق تخاصمي واحد بواحد. |

> **ما قبل هذا التاريخ غير مسجّل.** أول يوم للذرّة.
>
> **محاولة `1.2.0` الأولى (subscribe_all + core 1.14.0) نُفِّذت، اتختمت، ثم أُرجعت كاملة
> بأمر المالك المباشر «رجاع لقطة قبل فتح» — لأنها فُتحت يومها بلا إذن كافٍ.
> ثم أُعيد بناؤها نظيفة بنفس اليوم (٢٠٢٦-٠٨-١٩) بإذن المالك الصريح — انظر صفّ `1.2.0` أعلاه.

## 1.2.1 — 2026-08-20
- تنظيف مادة اللغة — الكود يتكلم رموزًا إنكليزية واللوحة تترجم؛ لا تغيير منطق. حُذفت تعليقات عربية فقط من atom.py؛ مضمونها مسجّل مسبقًا في هذا الملف.

- **حملة ١–٤٤٩ · الدفعة ب (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع اشتراكات الذرّة، رقم كريم مؤقّت قابل لضبط المالك من اللوحة لاحقًا. صفر تغيير في الكود والسلوك.

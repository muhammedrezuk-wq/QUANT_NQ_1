# Clock

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٣` |
| الاسم | Clock |
| المجلّد | `003_الساعة` |
| الإصدار | `3.0.3` |
| حرجة؟ | `true` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٢` ثانية |

**تسمع (من `manifest.yaml`):**
- `time.ntp.samples.state`

**تُرسِل (من `manifest.yaml`):**
- `time.utc.synced`
- `time.utc.drift`
- `time.clock.quality.state`
- `kernel.clock.sys_tick`
- `kernel.clock.heartbeat`
- `kernel.clock.minute_elapsed`

**إعداداتها:** `sys_tick_interval_s` · `heartbeat_interval_s` · `drift_alert_s` · `max_accepted_offset_s` · `max_sample_age_s` · `stale_after_s` · `max_slew_per_second`

<!-- BITAQA_END -->

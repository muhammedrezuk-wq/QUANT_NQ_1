# Probability Manager

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٣٥٠` |
| الاسم | Probability Manager |
| المجلّد | `350_مدير_الاحتمالات` |
| الإصدار | `3.3.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_SECOND`
- `probability.trend.state`
- `probability.reversal.state`
- `probability.breakout.state`
- `probability.pullback.state`
- `probability.momentum.state`
- `probability.range.state`
- `probability.hurst.state`
- `probability.merged.state`
- `probability.confidence.state`

**تُرسِل (من `manifest.yaml`):**
- `probability.cycle.collected`
- `probability.section.live`
- `probability.units.state`

**إعداداتها:** `timeout_seconds` · `section_weight` · `required_depth` · `confidence_threshold`

<!-- BITAQA_END -->

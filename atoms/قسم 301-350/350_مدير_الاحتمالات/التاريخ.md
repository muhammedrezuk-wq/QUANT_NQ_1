# التاريخ — 350_مدير_الاحتمالات

**الذرّة:** `٣٥٠` · **الإصدار:** `2.1.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.1.0` | **علاج مرضَي الصحّة المقيسَين** (قياس حيّ قبل الإصلاح: `late=24` ثابت عبر دقائق القياس — دفعة قديمة كانت تُبقي الذرّة «متعثّرة» للأبد رغم نظافة الحاضر): (١) **مبدأ الحداثة** — `health_check()` كانت تحكم على مجاميع العمر كلّه؛ صارت تحكم على **الجديد منذ آخر فحص** فقط (`_health_seen` ثم `new_X = total - seen`) والمجاميع باقية في التفاصيل — نسخ حرفي لنمط `400 v1.2.0` ونفس مبدأ `617 v3.0.1` المقيسَين. (٢) **تمييز الصدى** — `_recent` (نفس السعة 128، لا رقم جديد) صار يحمل مجموعة الوحدات المساهمة لكلّ دورة مغلقة بدل السلسلة وحدها: صدى وحدة ساهمت بدورة أُغلقت حديثًا حميد (يُحصى في `echoes` للتشخيص، لا يمسّ الصحّة)، ومتأخّر حقيقي لوحدة لم تساهم يبقى `late`. كذلك أُصلح ملف الاختبار (اختبار فقط): كان يبني المعرِّف بصيغة ثلاثية قديمة تخالف `cycle_key_of` الخماسي (نفس ما وثّقه ملف اختبار 359) — ٤ من ٦ اختبارات ساقطة أصلًا؛ أُصلح وأضيف ٣ حرّاس (تعافٍ بعد حادثة قديمة · صدى لا يُعدّ · متأخّر حقيقي يُعدّ) — النتيجة ٩/٩. ملاحظة نسخ: `manifest` كان `1.1.1` و`atom.py` كان `2.0.0` — انحراف موروث وُحِّد على `2.1.0`. |
| ٢٠٢٦-٠٨-١٨ | `1.1.1` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 22:04`

## 2.1.1 — 2026-08-20
- تنظيف مادة اللغة — الكود يتكلم رموزًا إنكليزية واللوحة تترجم؛ لا تغيير منطق. حُذفت تعليقات عربية فقط من atom.py؛ مضمونها مسجّل مسبقًا في هذا الملف.

## 3.0.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.


## 3.1.0 — 2026-08-23
**إقفال القسم 350 «الاحتمالات» · ذرّات أولًا (أمر المالك).**

- **انتهى عهد دورات التِكّة الفارغة:** مسار الفتح من الوحدات كان موجودًا أصلًا بالكود — أُسكِت مفتح التِكّة (نفس علاج 200/250/300). الدورة تُفتح من أول تسليم وحدة بهويتها وتكتمل 9/9.
- **جاهزية متدرجة:** `readiness_pct` على بطاقة القسم (العمق/العتبة كنسبة 0..100 — لا نطّات).
- **حدث جديد `probability.units.state`:** كل وحدة من التسع تعلن نفسها — ما قالت · ثقتها · احتمالها · تسليماتها · عمر آخر تسليم · إيقاعها ووقتها الجاي.
- الصحة صارت تحمل `units_tracked` · `panel_emitted` · `late` (كانت تحمل ticks).
- الاختبارات أُعيدت كتابتها: **4/4**.

# Risk Distribution

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦٦٣` |
| الاسم | Risk Distribution |
| المجلّد | `663_توزيع_المخاطر` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `risk.asset_ledger.state`

**تُرسِل (من `manifest.yaml`):**
- `portfolio.risk_distribution.state`

<!-- BITAQA_END -->

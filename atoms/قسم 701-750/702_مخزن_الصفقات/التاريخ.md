# التاريخ — 702_مخزن_الصفقات

**الذرّة:** `٧٠٢` · **الإصدار:** `2.4.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.4.1` | تصحيح دلالة الصحّة بأمر المالك («ما بدي أشوف شي متعثّر أبدًا — مو كذب، حقيقة»): `NO_TRADES_YET` كان يُعلن `DEGRADED` رغم أن المخزن جاهز وسليم ولا صفقة حدثت بالنظام أصلًا — جاهزية قبل أول عمل، لا عجز. صارت `HEALTHY` برسالة «جاهز — بانتظار أول صفقة تُخزَّن | stored=0». أعطال المخزن الحقيقية تبقى متعثّرة كما هي (`STORE_UNAVAILABLE`، `last_error`، تجاوز السقف)، وأُضيف فرع شكّ صريح: فشل تخزين مع صفر صفوف → `DEGRADED` لأن صفقات ربّما ضاعت. حُدّث حارس `test_health_states` ليثبّت الدلالة الجديدة. صفر تغيير سلوكي آخر. |
| ٢٠٢٦-٠٨-١٨ | `2.4.0` | **`max_db_bytes` كان مفقودًا من العقد والقيمة معًا** ← أُضيف بـ`1000000000` و`minimum: 1`. الحقل المفقود يعني بلا سقف تمامًا مثل الصفر، لكنّه لا يبدو خطأً عند القراءة — وهذا أخطر. حارسه: `governance/checks/check_storage_cap_contract.py` |
| ٢٠٢٦-٠٨-١٨ | `2.3.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 2.4.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- «شكّ عطل: %d فشل تخزين ولا صفّ مخزَّن» → `SUSPECTED_FAULT: %d store failures and zero rows stored`.
- رسالة الجاهزية: «جاهز — بانتظار أول صفقة تُخزَّن» → `READY_AWAITING_FIRST_TRADE_STORE`.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

# التاريخ — 706_مخزن_النماذج

**الذرّة:** `٧٠٦` · **الإصدار:** `2.1.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.1.1` | تصحيح دلالة الصحّة بأمر المالك («ما بدي أشوف شي متعثّر أبدًا — مو كذب، حقيقة»): `NO_ACTIVITY_YET` كان يُعلن `DEGRADED` رغم أن المخزن جاهز وسليم ولا نموذج أُنتج بالنظام بعد (لا صفقات مغلقة → لا تعلّم → لا نماذج) — جاهزية قبل أول عمل، لا عجز. صارت `HEALTHY` برسالة «جاهز — بانتظار أول نموذج يُحفَظ | saved=0 loaded=0». طلبُ تحميلٍ لنموذج غير موجود (`missing`) يبقى جاهزية لا عطلًا. أعطال المخزن الحقيقية تبقى متعثّرة كما هي، وأُضيف فرع شكّ صريح: فشل حفظ/تحميل مع صفر نشاط ناجح → `DEGRADED`. حُدّث حارس `test_health_states` ليثبّت الدلالة الجديدة. صفر تغيير سلوكي آخر. |
| ٢٠٢٦-٠٨-١٨ | `2.1.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 2.1.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- «شكّ عطل: %d فشل حفظ/تحميل ولا نشاط ناجح» → `SUSPECTED_FAULT: %d save/load failures and no successful activity`.
- رسالة الجاهزية: «جاهز — بانتظار أول نموذج يُحفَظ» → `READY_AWAITING_FIRST_MODEL_SAVE`.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

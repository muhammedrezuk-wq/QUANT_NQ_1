# Model Store

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٧٠٦` |
| الاسم | Model Store |
| المجلّد | `706_مخزن_النماذج` |
| الإصدار | `2.1.2` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `model.persist_requested`
- `storage.persistence.load_requested`

**تُرسِل (من `manifest.yaml`):**
- `storage.model_saved_confirmed`
- `model.persisted`
- `storage.persistence.load_response`
- `storage.persistence.save_failed`

**إعداداتها:** `db_path` · `max_db_bytes` · `keep_versions_per_model`

<!-- BITAQA_END -->

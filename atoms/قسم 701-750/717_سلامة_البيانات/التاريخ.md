# التاريخ — 717_سلامة_البيانات

**الذرّة:** `٧١٧` · **الإصدار:** `2.0.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.0.1` | تصحيح دلالة الصحّة بقاعدة المالك («متعثّرة» فقط لعجز فعلي): `NEVER_RAN` كانت `DEGRADED` والذرّة آخر حلقة سلسلة الصيانة (أرشفة `714` ← تنظيف `716` ← سلامة `717`) — حدثيّة بلا موعد خاص، تنتظر أول حدث تنظيف، ولا عمل فائتًا (السلسلة لم تُنتج حدثها بعد لأنّ لا بيانات بلغت عمر الأرشفة). صارت `HEALTHY` برسالة «جاهز — بانتظار أول حدث تنظيف قبل فحص السلامة &#124; runs=0». أعلام الفحص الفعلية (فساد · صفوف بلا ختم · صفوف مستقبلية) تبقى `DEGRADED` كما كانت. `health_check` فقط — صفر تغيير في سلوك الفحص، اختبارات 4/4 |
| ٢٠٢٦-٠٨-١٨ | `2.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 2.0.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- رسالة الجاهزية: «جاهز — بانتظار أول حدث تنظيف قبل فحص السلامة» → `READY_AWAITING_FIRST_CLEANUP_EVENT`.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

# Execution Lifecycle Store

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٧٠٧` |
| الاسم | Execution Lifecycle Store |
| المجلّد | `707_مخزن_القرارات` |
| الإصدار | `4.2.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `decision.approved.state`
- `perpetual.target.state`
- `decision.dispatch.state`
- `execution.order.requested`
- `execution.order.built`
- `execution.order.rejected`
- `trading.final_decision`
- `platform.brain_signal.written`
- `platform.brain_signal.write_failed`
- `execution.command.ack`
- `execution.command.failed`
- `platform.trade_event`

**تُرسِل (من `manifest.yaml`):**
- `storage.decisions_saved`

**إعداداتها:** `db_path` · `keep_full_payload` · `max_rows` · `max_db_bytes`

<!-- BITAQA_END -->

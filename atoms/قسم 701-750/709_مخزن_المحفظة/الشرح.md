# Portfolio Store

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٧٠٩` |
| الاسم | Portfolio Store |
| المجلّد | `709_مخزن_المحفظة` |
| الإصدار | `2.4.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `portfolio.summary`
- `portfolio.overview.state`
- `SYS_DAY`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `storage.portfolio_saved`

**إعداداتها:** `db_path` · `min_write_interval_s` · `retention_days` · `max_rows` · `max_db_bytes`

<!-- BITAQA_END -->

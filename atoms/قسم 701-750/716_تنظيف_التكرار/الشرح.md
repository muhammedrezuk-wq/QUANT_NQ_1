# Duplicate Cleanup

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٧١٦` |
| الاسم | Duplicate Cleanup |
| المجلّد | `716_تنظيف_التكرار` |
| الإصدار | `2.1.2` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `storage.archived`

**تُرسِل (من `manifest.yaml`):**
- `storage.trading_data_cleaned`

**إعداداتها:** `stores` · `vacuum_after_cleanup`

<!-- BITAQA_END -->

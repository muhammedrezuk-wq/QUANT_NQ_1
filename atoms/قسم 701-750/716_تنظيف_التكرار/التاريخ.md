# التاريخ — 716_تنظيف_التكرار

**الذرّة:** `٧١٦` · **الإصدار:** `2.1.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.1.1` | تصحيح دلالة الصحّة بقاعدة المالك («متعثّرة» فقط لعجز فعلي): `NEVER_RAN` كانت تُعلن `DEGRADED` وتُخفي حقيقتين معًا: (أ) الذرّة حدثيّة تنتظر حدث الأرشفة — لا موعد خاصًّا بها؛ (ب) اللقطة المستعادة تحمل `next_store_index=1` — جولة سابقة **لم تكتمل** (توقّفت عند مخزن `analysis`؛ فشل أو إيقاف النواة أثناءها — لا سجلّ خطأ محفوظًا للجزم). قِيس اليوم: المخازن الخمسة موجودة، `analysis` فيه 29,410 صفًّا و**صفر تكرار** (المسح 15 ثانية) — لا عجز قائمًا، والاستئناف مُسلَّح تلقائيًّا للحدث القادم. صارت `HEALTHY` برسالتين صادقتين: «جاهز — بانتظار أول حدث أرشفة &#124; runs=0» أو — عند استئناف معلّق — «جاهز — جولة سابقة لم تكتمل؛ الاستئناف من مخزن X (i من n) عند حدث الأرشفة القادم &#124; runs=0 resume=i» (لا إخفاء للجولة الناقصة). أخطاء التنظيف الفعلية تبقى `DEGRADED`. `health_check` فقط — منطق التنظيف لم يُمسّ عملًا بورقة ٩٠ بند ٣، اختبارات 4/4 |
| ٢٠٢٦-٠٨-١٨ | `2.1.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 2.1.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- «جاهز — جولة سابقة لم تكتمل؛ الاستئناف من مخزن %s (%d من %d) عند حدث الأرشفة القادم» → `READY - previous round incomplete; resume from store %s (%d of %d) on next archive event`.
- رسالة الجاهزية: «جاهز — بانتظار أول حدث أرشفة» → `READY_AWAITING_FIRST_ARCHIVE_EVENT`.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

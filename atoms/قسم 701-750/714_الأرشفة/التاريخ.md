# التاريخ — 714_الأرشفة

**الذرّة:** `٧١٤` · **الإصدار:** `3.0.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `3.0.1` | تصحيح دلالة الصحّة بقاعدة المالك («متعثّرة» فقط لعجز فعلي): رسالة `SKIPPED_WINDOW WITHIN_WINDOW` كانت تُعلن `DEGRADED` رغم أنّ معناها المقيس أنّ آخر أرشفة ناجحة عمرها أقلّ من نافذة الـ24 ساعة (مُثبَت حيًّا: `last_success` قبل 6.1 ساعة) وأنّ اللحاق قرّر صوابًا ألّا يعيد التشغيل — لا عجز ولا موعد فائتًا. صارت `HEALTHY` برسالة صادقة «جاهز — آخر أرشفة ناجحة قبل X ساعة عند الإقلاع (داخل نافذة 24 ساعة) &#124; WITHIN_WINDOW runs=0». حالة ما قبل أول نبضة صارت رسالتها الصريحة `AWAITING_FIRST_PULSE` بدل `NEVER_RAN` الموهمة، وتبقى `DEGRADED` لأنّ استمرارها يعني غياب الوقت الرسمي — عطل حقيقي لا يُخفى. حارسان حُدّثا: `verdict_cycle` يعدّ `AWAITING_FIRST_PULSE` فشلًا كما كان يعدّ `NEVER_RAN`، و`check_archive_catchup_contract` كان **ساقطًا فعلًا** (يحمّل `catchup.py` من مجلد الذرّة وقد انتقلت الوحدة إلى حزمة `catchup/` بالجذر) فأُصلح مساره وعاد أخضر (22 فحصًا · 0 اختلاف). `health_check` فقط — صفر تغيير في سلوك الأرشفة، اختبارات 5/5 |
| ٢٠٢٦-٠٨-١٨ | `3.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 3.0.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- «جاهز — آخر أرشفة ناجحة قبل %s ساعة عند الإقلاع (داخل نافذة 24 ساعة)» → `READY - last successful archive %s hours ago at startup (within 24h window)`.
- ذيل رسالة AWAITING_FIRST_PULSE: «لا نبضة وقت رسمي بعد؛ قرار اللحاق لم يُحسم» → `no official time pulse yet; catch-up decision unresolved`.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 60` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

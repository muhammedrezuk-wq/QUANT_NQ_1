# التاريخ — 712_مخزن_التحليل

**الذرّة:** `٧١٢` · **الإصدار:** `2.3.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.3.1` | **إمساك السقف وسط الفيضان (نزيف حي مقيس ~9GB/ساعة):** إنفاذ السقف كان يعمل `VACUUM` داخل سيل الكتابة فيقفل الكاتب دقائق («database is locked») ويخسر السباق، وقصّ الـWAL يفشل ما دام للوحة قارئ دائم فانفلت بالجيغابايتات. العلاج في `storage_policy` المشترك: المسار الساخن يحذف الأقدم ويقيس **المحتوى الفعلي** (الصفحات المستعملة — الحذف وحده يوقف نموّ الملف بإعادة استعمال الصفحات) بلا VACUUM، وحدّ `journal_size_limit` بنفس سقف المخزن المصرّح (لا رقم جديد)؛ والتقليص الفعلي (`shrink=True`: قصّ WAL + VACUUM) صار حصرًا مع الدورة اليومية الهادئة. |
| ٢٠٢٦-٠٨-١٨ | `2.3.0` | **`max_db_bytes: 0` ← `2000000000`** بأمر المالك، و`minimum: 0` ← `1` في العقد فلا يعود الصفر قيمةً صالحة. المقيس يوم التعديل: ٣٧١٬٨١٣ صفّ في ٠٫٨٤ يوم = **١١٫٨٧ جيجا/يوم** و**٢٨٫٠ كيلوبايت للصفّ** (١٥ ضعف جارتيها ٧١٣/٧١٨) — فسقف الصفوف `1000000` أوّل ما يعضّ عند **٢٨ جيجا**، و`retention_days: 90` يحتاج ٨٩ يومًا، و`archive_after_days: 60` في ٧١٤ يحتاج ٦٠. لا شيء يصل في الوقت؛ سقف البايت وحده يحدّ مخزنًا حجم صفّه غير معروف سلفًا. حارسه: `governance/checks/check_storage_cap_contract.py`. والقاعدة أُرشفت كاملة قبل التعديل (`سطح المكتب\ارشيف التنظيف — QUANT_NQ — 2026-08-18\قواعد_قبل_وضع_السقوف`) — ٩٫٩٣ جيجا · فحص سلامة `ok` |
| ٢٠٢٦-٠٨-١٨ | `2.2.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

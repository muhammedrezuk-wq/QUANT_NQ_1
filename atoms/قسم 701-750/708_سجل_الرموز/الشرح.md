# Symbol Registry And Resolution

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٧٠٨` |
| الاسم | Symbol Registry And Resolution |
| المجلّد | `708_سجل_الرموز` |
| الإصدار | `2.5.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `storage.symbol.resolve_requested`
- `symbol.resolve.requested`
- `market.symbol_specs`
- `market.ctrader.symbol_specs`
- `platform.account.state`

**تُرسِل (من `manifest.yaml`):**
- `storage.symbol.map.state`
- `storage.symbol.resolved`
- `storage.symbol.unmapped`
- `symbol.resolve.result`

**إعداداتها:** `canonical_map` · `canonical_patterns` · `broker_map` · `min_stem_length` · `strip_suffixes` · `passthrough_unknown`

<!-- BITAQA_END -->

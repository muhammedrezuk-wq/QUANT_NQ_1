# Archive Compression

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٧١٥` |
| الاسم | Archive Compression |
| المجلّد | `715_ضغط_الأرشيف` |
| الإصدار | `3.0.2` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `storage.archived`

**تُرسِل (من `manifest.yaml`):**
- `storage.compressed`

**إعداداتها:** `min_size_bytes` · `compression_level` · `keep_original` · `active_archive_db_path`

<!-- BITAQA_END -->

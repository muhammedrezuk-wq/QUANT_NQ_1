# التاريخ — 703_مخزن_الأوامر

**الذرّة:** `٧٠٣` · **الإصدار:** `3.1.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `3.1.1` | تصحيح دلالة الصحّة بأمر المالك («ما بدي أشوف شي متعثّر أبدًا — مو كذب، حقيقة»): `NOTHING_STORED_YET` كان يُعلن `DEGRADED` رغم أن المخزن جاهز وسليم ولا أمر بُني بالنظام أصلًا — جاهزية قبل أول عمل، لا عجز. صارت `HEALTHY` برسالة «جاهز — بانتظار أول أمر يُخزَّن | stored=0». أعطال المخزن الحقيقية تبقى متعثّرة كما هي (`STORE_UNAVAILABLE`، `last_error`، تجاوز السقف)، وأُضيف فرع شكّ صريح: أوامر أُسقطت مع صفر مخزَّن → `DEGRADED` لأن هذا عرَض فقدان حقيقي. حُدّث حارس `test_health_states` ليثبّت الدلالة الجديدة. صفر تغيير سلوكي آخر. |
| ٢٠٢٦-٠٨-١٨ | `3.1.0` | شدّ العقد: و`minimum: 0` ← `1` في العقد فلا يعود الصفر قيمةً صالحة — الثغرة التي جعلت مخزنًا بلا سقف مخزنًا **صالحًا**. القيمة كانت سليمة أصلًا (١ جيجا). حارسه: `governance/checks/check_storage_cap_contract.py` |
| ٢٠٢٦-٠٨-١٨ | `3.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 3.1.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- «شكّ عطل: %d أمر أُسقط ولا شيء مخزَّن» → `SUSPECTED_FAULT: %d orders dropped and nothing stored`.
- رسالة الجاهزية: «جاهز — بانتظار أول أمر يُخزَّن» → `READY_AWAITING_FIRST_ORDER_STORE`.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 60` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

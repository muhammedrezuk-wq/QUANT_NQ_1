# Order Store

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٧٠٣` |
| الاسم | Order Store |
| المجلّد | `703_مخزن_الأوامر` |
| الإصدار | `3.1.2` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `execution.order.built`
- `SYS_DAY`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `storage.orders_saved`

**إعداداتها:** `db_path` · `flush_size` · `retention_days` · `flush_interval_s` · `max_rows` · `max_db_bytes`

<!-- BITAQA_END -->

# Time Bridge

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `١١١` |
| الاسم | Time Bridge |
| المجلّد | `111_جسر_الوقت` |
| الإصدار | `3.0.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `time.utc.drift`
- `time.utc.synced`
- `kernel.clock.heartbeat`

**تُرسِل (من `manifest.yaml`):**
- `market.time.drift`
- `market.time.synced`
- `time.clock.divergence`

**إعداداتها:** `max_age_s` · `divergence_threshold_s`

<!-- BITAQA_END -->

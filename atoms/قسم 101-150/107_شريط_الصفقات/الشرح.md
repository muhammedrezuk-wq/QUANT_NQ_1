# Trade Tape Receiver

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `١٠٧` |
| الاسم | Trade Tape Receiver |
| المجلّد | `107_شريط_الصفقات` |
| الإصدار | `2.0.1` |
| حرجة؟ | `false` |
| الإقلاع | `manual` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.trade`

**تُرسِل (من `manifest.yaml`):**
- `market_data.trade_tape_updated`

**إعداداتها:** `source_event` · `window_size` · `publish_every_n_trades`

<!-- BITAQA_END -->

# Validated Market Tick Gate

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `١١٢` |
| الاسم | Validated Market Tick Gate |
| المجلّد | `112_مدقق_البيانات` |
| الإصدار | `4.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.tick`
- `platform.account.state`

**تُرسِل (من `manifest.yaml`):**
- `market.tick.validated`
- `market_data.validation_failed`
- `market_data.validation.state`

<!-- BITAQA_END -->

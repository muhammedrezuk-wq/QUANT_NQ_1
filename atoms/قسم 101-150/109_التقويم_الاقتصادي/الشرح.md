# Economic Calendar Receiver

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `١٠٩` |
| الاسم | Economic Calendar Receiver |
| المجلّد | `109_التقويم_الاقتصادي` |
| الإصدار | `3.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.calendar`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `market_data.calendar_event`
- `market_data.calendar_window`
- `market_data.calendar.state`

**إعداداتها:** `source_event` · `alert_before_seconds` · `keep_past_seconds` · `min_impact`

<!-- BITAQA_END -->

# Feed Interruption Detector

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `١١٦` |
| الاسم | Feed Interruption Detector |
| المجلّد | `116_كشف_الانقطاع` |
| الإصدار | `3.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `feed.mt5.tick`
- `feed.ctrader.tick`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `market_data.feed_interrupted`
- `market_data.feed_recovered`
- `market_data.feed.state`

**إعداداتها:** `max_silence_seconds` · `dead_after_seconds`

<!-- BITAQA_END -->

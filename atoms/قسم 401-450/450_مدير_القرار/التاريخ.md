# التاريخ — 450_مدير_القرار

**الذرّة:** `٤٥٠` · **الإصدار:** `1.1.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٠ | `1.1.0` | **بختم NQ بند 22 حزمة ب — ق٨: مدير دورة القرار وسجلّها الموحد — لا يخترع قرارًا.** جديد: بطاقة `decision.cycle.record` لكل معرّف قرار (وعند غيابه بالنشر المتدرج: ربط بمعرّف الدورة الحقيقي + إعلان `identity_incomplete` — لا اختراع) فيها: القرار النهائي **حين يصل من 458** (`decision.resolved.state` اشتراك جديد — 450 يستلمه ولا يصنعه) + أهلية الشراء وسببها (455) + أهلية البيع وسببها (456) + حالة الانتظار وسببها (457) + الهوية الست المدموجة بلا كتابة فوق معلوم + أوقات كل مرحلة (`source_timestamp` + `received_at` بالساعة الرسمية) — للعرض والسجل، لا قرار فيها. السطح القديم للوحة (`decision.cycle.collected`) باقٍ كما هو مع تبديل وحدتي الشراء/البيع الملغاتين بحدثي الأهلية الجديدين (`decision.eligibility.buy.state` و`decision.eligibility.sell.state`؛ `decision.wait.state` بقي اسمه). سعة السجل 256 بطاقة بإزاحة الأقدم. اختبارات: السجل الموحد + §١٩ (المؤهلان يبقيان والحسم من 458 وحده) + النقص المعلَن + السطح القديم يكتمل (5/5). |
| ٢٠٢٦-٠٨-١٨ | `1.0.1` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 21:28`

## 1.2.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** سُمّي الرقم السحري `[3]` في `_forward` — صار `_SCOPE_SYMBOL_INDEX` بموثيقه. صفر تغيير منطقي، الاختبارات 5/5، وصفر مخالفات وصفر تحذيرات.

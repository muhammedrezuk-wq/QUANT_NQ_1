# التاريخ — 409_استراتيجية_المدى

**الذرّة:** `٤٠٩` · **الإصدار:** `1.1.2`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `1.1.2` | إصلاح عطل مقاس: الإشارة كانت تُنشر **بلا حقل `score`** بينما عقد التجميع يقرؤه — `451` يمتصّ `unit.get("score")` (الغياب = صفر) و`453` يضرب الدرجة في المساهمة، فمساهمة الذرّة كانت تُحسب **صفرًا دائمًا**. أُضيف الحقل بالدلالة المعتمدة: `100.0` عند إشارة فعلية و`0.0` عند لا-إشارة — «الإشارة كاملةٌ والثقةُ تعيّرها»، فلا قناعة تُحسب مرّتين. حارس `test_contract_shape_complete` كان يسقط على الغياب (مقاس قبل الإصلاح) وأُضيف تثبيت القيمتين في اختباري الإشارة والصمت |
| ٢٠٢٦-٠٨-١٨ | `1.1.1` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-18 02:10`

## 2.0.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

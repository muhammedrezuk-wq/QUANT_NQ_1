# التاريخ — 411_استراتيجية_الأخبار

**الذرّة:** `٤١١` · **الإصدار:** `2.1.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٥ | `2.1.0` | **الذرّة ما عادت تلمس الساعة إطلاقًا — بختم `NQ`:** كانت تقرأ ساعة الجهاز في موضعين. **(أ)** خبر بلا وقت نشر ولا ختم كان يُوسَم بـ`time.time()`؛ صار وقته **مجهولًا معلَنًا** (عدّاد `no_time` بالصحّة واللقطة)، ويبقى بالدفتر للسجلّ **ولا يُحسب أبدًا ضمن نافذة الطزاجة** لأنّه لا يستطيع الإجابة عن «هل وقع خلال ±١٥ دقيقة؟». وتُعلَن العدّة `untimed_news` ببطاقة القسم. **(ب)** ساعة احتياطيّة على التِكّة كانت غير قابلة للوصول أصلًا (`ingest` يرفض تِكّة بلا ختم) — أُزيلت بدل تركها قراءةَ ساعة نائمة، والتِكّة بلا وقت تُرفض وتُعدّ. **والقاعدة ١٣ صارت مستوفاة: صفر قراءة ساعة بالذرّة.** |
| ٢٠٢٦-٠٨-٢٠ | `1.2.1` | **إعادة بناء صادقة بختم NQ بند 22 حزمة أ — ق٧:** الخبر يُربط بـ«نطاق تأثير» (رموزه) لا برمز منفرد، وبثلاث درجات مشتقّة من `impact_level` الموجود (`high`/`عالي`→عالٍ · `medium`→متوسّط · غيرها→خفيف — لا اختراع). (١) اشتراك جديد بـ`market.news.enriched` (الغنيّ من 615) إضافةً للمصدر القائم، مع منع التكرار بمعرّف الإغناء وبالعنوان المطبّع — معرّفات المصدر الرشيق لا تُستعمل مفاتيح إسقاط لأن تسلسلها من قاعدة أخرى. (٢) مخزن أخبار **لكل رمز** بدل النافذة العالمية؛ شمعة الرمز تُجاب من أخبار رمزها وحده، والمجهول النطاق يبقى معدودًا ظاهرًا ولا يُلصق برمز. (٣) غياب الأخبار **لم يعد حيادًا مقيسًا**: `score=None` بدل `0` فيعلن العقد `direction` مجهولًا في `unknown_fields` — «المجهول ليس حيادًا». (٤) حدث جديد `news.trading_window.state` لكل (رمز، خبر نشط ضمن نافذته): درجة عالية ⇒ `block=true` ضمن 15 دقيقة قبل/بعد (الرقم الوحيد المعطى بحكم ق٧)؛ المتوسّط/الخفيف صفر = بلا نافذة حتى يضبطها المالك. (٥) أربعة عيارات مالك جديدة بسجلّ المُعامِلات (`NEWS_HIGH_WINDOW_BEFORE/AFTER_MIN=15` · `NEWS_MEDIUM/LIGHT_WINDOW_MIN=0`) تُقرأ بـ`effective_value` وتُطبَّق حيًّا عبر `decision.settings.command` (نمط 458 الحرفي) مع نشر `decision.settings.state`. (٦) `source_timestamp` حقيقي بكل حمولة (زمن نشر الخبر للنوافذ، لحظة الحساب للحالة) فتشيخ الحمولات بصدق. |
| ٢٠٢٦-٠٨-٢٠ | `1.2.0` | **وُصِلت بالمغذّي** بأمر المالك (ختم `NQ`): أُضيف اشتراك بـ`market_data.news_received`. قبلها كانت تشترك بإغلاق الشمعة **فقط** فتصحو ولا خبر عندها وتعلن `NEWS_UNAVAILABLE` دائمًا — **والسلك للمجمّع كان موجودًا** (411 ← 400 ← 451) والمغذّي `108` ينشر فعلًا (`published=50` مقيسًا)، لكنّها لم تكن مشتركة بتيّاره. صارت تحتفظ بنافذة زمنيّة (`recent_window_s`) وتعلن عند كل شمعة: عدد الأخبار وأعلى أثر وآخر عنوان وعمره. **ولا تدّعي اتجاهًا:** `signal` يبقى `none` و`score=0` و`confidence=0` مهما كان العنوان — المغذّي لا يحمل معنى اتجاهيًّا. سبعة اختبارات (٣ قديمة + ٤ جديدة، منها حارس «لا اتجاه مخترع» وحارس سقوط الخبر خارج النافذة) · المدقّق صفر مخالفة |
| ٢٠٢٦-٠٨-١٨ | `1.1.1` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-18 02:10`

## 2.0.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

# News Strategic Regime

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٤١١` |
| الاسم | News Strategic Regime |
| المجلّد | `411_استراتيجية_الأخبار` |
| الإصدار | `2.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.tick.validated`
- `market_data.news_received`
- `market.news.enriched`

**تُرسِل (من `manifest.yaml`):**
- `strategy.news.state`
- `news.trading_window.state`

**إعداداتها:** `weight` · `required_depth` · `confidence_threshold` · `strength_threshold` · `required_ticks` · `recent_window_s` · `high_window_before_min` · `high_window_after_min`

<!-- BITAQA_END -->

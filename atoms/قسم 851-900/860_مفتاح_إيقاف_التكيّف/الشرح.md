# Adaptation Kill Switch

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٨٦٠` |
| الاسم | Adaptation Kill Switch |
| المجلّد | `860_مفتاح_إيقاف_التكيّف` |
| الإصدار | `1.0.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `drift.vector.state`
- `measurement.latency.state`
- `adaptation.kill_switch.command`

**تُرسِل (من `manifest.yaml`):**
- `adaptation.kill_switch.state`

**إعداداتها:** `critical_drift` · `latency_budget_ms` · `hot_path_sections`

<!-- BITAQA_END -->

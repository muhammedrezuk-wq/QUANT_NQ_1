# Durable Execution Confirm

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٦٣` |
| الاسم | Durable Execution Confirm |
| المجلّد | `563_تأكيد_التنفيذ` |
| الإصدار | `3.1.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `platform.trade_event`
- `trading.final_decision`
- `market.symbol_specs`
- `platform.account.state`

**تُرسِل (من `manifest.yaml`):**
- `market.outcome.realized`
- `execution.command.ack`
- `execution.confirmation.rejected`

**إعداداتها:** `dedupe_db_path`

<!-- BITAQA_END -->

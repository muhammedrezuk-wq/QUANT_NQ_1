# Multi-Level Tilt Engine

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٨٠` |
| الاسم | Multi-Level Tilt Engine |
| المجلّد | `580_منفذ_الترجيح` |
| الإصدار | `1.2.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `decision.gate.passed`
- `tilt.rule.command`

**تُرسِل (من `manifest.yaml`):**
- `tilt.state`
- `tilt.rules.state`

**إعداداتها:** `db_path` · `tilt_max_total`

<!-- BITAQA_END -->

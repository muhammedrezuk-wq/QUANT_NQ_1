# التاريخ — 584_شرعية_الستوب

**الذرّة:** `٥٨٤` · **الإصدار:** `1.5.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `1.5.1` | تصحيح دلالة الصحّة بأمر المالك («ما بدي أشوف شي متعثّر أبدًا — مو كذب، حقيقة»): `legal=0 rejected=0` كان `DEGRADED` رغم أن النظام لم يبنِ أي أمر بعد ليُفحص ستوبه — جاهزية لا عجز. صارت `HEALTHY` برسالة «جاهز — بانتظار أول أمر مبني لفحص الستوب \| legal=0 rejected=0». `NOT_STARTED` يبقى عجزًا فعليًّا. صفر تغيير سلوكي آخر. |
| ٢٠٢٦-٠٨-١٨ | `1.5.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 1.5.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- رسالة الجاهزية: «جاهز — بانتظار أول أمر مبني لفحص الستوب» → `READY_AWAITING_FIRST_ORDER_STOP_CHECK`.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

- **ملاحظة تحقُّق (٢٠٢٦-٠٨-٢٧ — بند ٢٢/٢٧ بمراجعة ٢٧ ذرّة، بلا تغيير كود هنا):**
  ثبت بتتبُّع `manifest.yaml` لكل من `552`/`601` أنّ هذه الذرّة **بوّابة
  حقيقية** على مسار التنفيذ الفعلي — لا مراقب مواز: `552` (التي تنشر
  `trading.final_decision` نحو الكاتب الحقيقي `601`) تشترك بحدث
  `execution.order.legal` **الناتج عن هذه الذرّة فقط**، لا بـ
  `execution.order.built` الخام من `551`. فأمرٌ بستوب مشوَّه من مسار
  `551` المباشر (غير محروس هناك، راجع تاريخ `551` v4.3.1) يُرفَض هنا
  فعليًّا قبل الوصول لتنفيذ حقيقي. اختبار طرف لطرف بكود `551`/`584`
  الحقيقي يثبت هذا بملفّ اختبار `551`. لا حاجة لأي تعديل بهذه الذرّة.

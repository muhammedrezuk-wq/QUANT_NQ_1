## 5.0.2 — 2026-08-20
- **تنظيف لغة الكود (م3/ملف4 — عربي داخل atom.py):** رسالة جاهزية الصحّة: «جاهز — بانتظار أول أمر شرعي» ← `READY_AWAITING_FIRST_LEGAL_ORDER` (الذيل `| finalized=0 rejected=0` كما هو). صفر تغيير في المنطق.


- **2026-08-15 · v2.8.1 — تعديل إعداد من اللوحة:** enabled=True (رفع التصحيح تلقائي — شرط النواة لإعادة التحميل الحي).

- **2026-08-15 · v2.8.2 — تعديل إعداد من اللوحة:** enabled=False (رفع التصحيح تلقائي — شرط النواة لإعادة التحميل الحي).

- **٢٠٢٦-٠٨-١٩ · v5.0.1 — تصحيح دلالة الصحّة بأمر المالك** («ما بدي أشوف شي متعثّر أبدًا — مو كذب، حقيقة»): `NO_INPUT_YET` كان `DEGRADED` رغم أن النظام لم يبنِ أي أمر شرعي بعد — سلسلة التنفيذ بلا عمل بحكم الواقع، والذرّة سليمة جاهزة. صارت `HEALTHY` برسالة «جاهز — بانتظار أول أمر شرعي | finalized=0 rejected=0» مع بقاء كل العدّادات بالتفاصيل. `NOT_STARTED` يبقى عجزًا فعليًّا. صفر تغيير سلوكي آخر.

## 5.1.0 — 2026-08-21
**حزمة ت (ت١+ت٣) — ضمن ختم البند 22.** الأمر المقفول صار يحمل هوية قراره، وكل رفض صار يسمّي نفسه:
- **ت١ — الهوية:** `decision_id` و`gate_request_id` حقلان رسميّان بالأمر المقفول. الغائب يمرّ `None` **معلَنًا** (`identity_missing` + `identity_warnings=['identity_incomplete']`) — لا اختراع ولا صفر ملفَّق.
- **ت١ — لحم الخيط المقطوع:** حين يسقط 551 الهوية، تُسترجَع من سجل اللقطة الموثوق (583) بمفتاح `snapshot_id` غير القابل للتغيير، ويُعلَن `identity_from_snapshot=true` مع عدّاد `_identity_recovered`. الخيط لا ينقطع عند 551.
- **ت٣ — الرفض يسمّي مرحلته:** خمس مراحل معلَنة (`FINAL_VALIDATION` · `ASSET_ACTIVATION` · `PARENT_DECISION` · `MARGIN_VERDICT` · `SNAPSHOT_VALIDITY`)، وكل رفض يحمل رباعية الحاجز `{value, threshold, reason, measured_at}` — والقيمة والعتبة تبقيان `None` حيث لا قياس حقيقيّ عند هذه البوابة (قانون حواجز 454 نفسه).
- **مدخلان جديدان بالمانيفست:** `risk.margin.validation.completed` (حكم الهامش من 585) و`execution.snapshot.state` (لقطة 583) — يُحفظان بذاكرة محدودة بـ`_MAX_TRACKED=4096` (حدّ ذاكرة، ليس قيمة تداول: الأقدم يُزاح).
- أوامر الإدارة والإغلاق تبقى **معفاة** من فحص القرار الأب — لا يكسرها الحاجز الجديد.
- **مقيس:** ملفّ اختبار الذرّة يمرّ كاملًا تحت المشغّل الرسميّ، والذرّة **حيّة 5.1.0 سليمة** (`READY_AWAITING_FIRST_LEGAL_ORDER`).
- **مخالفة معلَنة قائمة:** الذرّة صارت **361 سطرًا فعليًّا > 350** — مخالفة بمدقّق م9/ملف4، مرفوعة لحكم المالك (استثناء موثَّق أم تقسيم). لم تُستثنَ من عندي.

## 5.2.0 — 2026-08-21
**بقية حزمة ت — ت٤ (فجوات التخزين الدائم) — ضمن ختم البند 22.** فجوة مقيسة: كاشا استرجاع الهوية اللذان بنتهما ت١ (`_snapshots` من 583 و`_margin_verdicts` من 585) لم يكونا يعبران `snapshot()`/`restore()` — كانا يُصفَّران عند كل إقلاع بينما حالة الإيقاف (`global_halted`/`halted_accounts`) وحدها كانت تنجو. صار الاثنان يُخزَّنان ويُستعادان بنفس الحد الموجود أصلًا (`_MAX_TRACKED=4096` — حدّ ذاكرة، ليس قيمة تداول)، بلا أي حقل جديد مخترَع وبلا مسّ لصفة «الغياب معلَن لا مُخترَع» (سجل ناقص/تالف بحالة الاسترجاع يُتجاوز بصمت — لا يُسقط قفل الأمان `FAIL_CLOSED` الذي يبقى محصورًا بحالة الإيقاف وحدها). **مقيس:** اختباران جديدان — استرجاع كامل يكمّل هوية أمر بعد "إقلاع" مُحاكى، واسترجاع حالة قديمة بلا الحقلين الجديدين لا يكسر شيئًا (توافق خلفي). ملفّ اختبار الذرّة يمرّ كاملًا تحت المشغّل الرسميّ. **الفجوة الأوسع (بقية سلسلة التنفيذ 450–458/577/583) فُحصت وتبيّن أنها بتصميم متعمَّد لا فجوة:** `decision_id` يُشتقّ حتميًّا من `cycle_key` في 451 نفسها («تعليق ب1: إعادة تشغيل الدورة لا تكرّر قرارًا»)، وذاكرات إزالة التكرار الحسّاسة (467 الهوية، 516 الحجز/اليوميّ) محفوظة أصلًا بـ`snapshot`/`restore` مُثبَتَين — لم يُمسّا. صفر رقم سحري جديد، وصفر إعادة بناء لما يعمل.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 60` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

## 5.3.0 — 2026-08-24 — Patch قبل الختم
- فصل snapshot/restore/health إلى خدمة مشتركة؛ atom.py دون حد 350.

- 2026-08-25 · v5.3.1 — the enabled gate survives restart (snapshot carries it; was fail-open: every boot reopened a closed gate). (nq seal)

- 2026-08-25 · v5.4.0 — the _running gate is REMOVED from the whitelist STATE handler (468 publishes at its start, before 552's start in boot order — the only whitelist of the boot was swallowed and six built orders died SYMBOL_NOT_ALLOWED while the list said BTCUSD allowed; measured live). Whitelist state now visible in health details. (nq seal)

- 2026-08-25 · v5.4.1 — the _running gate is removed from ALL state handlers (account/halt/reset/specs/tick/margin/snapshot/reconcile/exposure/reference/rehydrate): subscribe-time replay arrives before start(), so every hot swap lost the replayed state (measured live twice: whitelist then RECONCILIATION_NOT_MATCHED). Decision handlers keep the gate. (nq seal)

- 2026-08-25 · v5.4.2 — execution.gate.state now carries open (enabled and not halted): the dashboard banner read gate?.open which never existed on the wire, so it said closed forever (documented display lie — fixed at the wire, not the UI). (nq seal)

## 5.5.0 — 2026-08-27 (بند ٢٥/٢٧ بمراجعة ٢٧ ذرّة: كودٌ ميت خطر، ثمّ إصلاحه ووصله)

**الاكتشاف.** `order_gates.py` و`state_inputs.py` بمجلّد هذه الذرّة **غير
مستوردَين من أي مكان بالمشروع كلّه** (تأكّد بحثًا شاملًا لا بالمجلّد وحده).
بقايا "حملة ٤٥٠–٩٠١ حزمة ب" (٢٠٢٦-٠٨-٢٤، v5.3.0): استُخرج منها
snapshot/restore/health فعلاً ويعمل اليوم (`shared/order_validator_state.py`)،
لكن استخراج حرّاس الحالة الخمسة وبوّابات فتح الأمر توقّف في المنتصف —
`atom.py` أبقى نسختيه الداخليّتين الكاملتين، وبقي الملفّان منسيّين.

**لماذا خَطِران تحديدًا.** `state_inputs.py` نسخة **مجمَّدة من قبل** إصلاحي
v5.4.0/v5.4.1 أعلاه بيوم واحد: حارس `self._running` على حرّاس الحالة
الخمسة كلّها — نفس العلّة المقيسة حيّاً مرّتين (قائمة بيضاء بلعت أول
قائمتها، ثمّ RECONCILIATION_NOT_MATCHED). لو وُصل الملف يومًا بمحاولة
تنظيف مستقبليّة، تعود العلّتان بصمت. زيادة: `_remember()` بداخله بلا حدّ
ذاكرة إطلاقًا، خلافًا لنسخة `atom.py` (`_MAX_TRACKED=4096`).

**قرار المالك.** عُرض الخيار: حذف أم إصلاح مع الإبقاء (لا git بهذا
المشروع، فالحذف حُكم عليه بأنه أصعب من أن يُنقَض — رفضته أداة التنفيذ
التلقائي فعلاً حين حاولته). اختار المالك: **أصلحهما إصلاحًا حقيقيًا لا
ديكورًا**، ثمّ افحصهما، ثمّ توقّف. لاحقًا طلب فحص إمكانيّة **وصلهما فعليًا**
بدل إبقائهما مُصلَحين معلَّقين، بعد شرح كامل ومحاكاة أخيرة، ثمّ أمر بالتطبيق.

**الإصلاح (قبل أي وصل):**
1. `state_inputs.py`: حارس `self._running` أُزيل من الدوال الخمس (مطابقًا
   لكود `atom.py` بعد v5.4.1 حرفيًا). `_remember()` صار محدودًا بـ
   `_MAX_TRACKED=4096` مع إزاحة الأقدم، مطابقًا لنسخة `atom.py`.
2. `order_gates.py`: مفتاح البحث عن حكم الهامش كان يستخدم
   `body.get("account_id")` **خامًا بلا `text()`** بينما كل كتابة لنفس
   السجل تُخزَّن بعد `text()` — كان يعمل بالصدفة فقط لأن `_on_built`
   تُطبَّع `account_id` قبل وصوله لهذه الدوال، ولا شيء بالملف نفسه يضمن
   ذلك. أُصلح بتغليف البحث بـ`text()` — يطابق الآن سلوك `atom.py` الأصلي
   تمامًا (`text()` بلا أثر على قيمة مُطبَّعة أصلًا)، لا سلوكًا جديدًا.

**أُثبت الإصلاح تجريبيًا** بمعزل عن أي وصل: سكربت مستقلّ يستدعي الدوال
الخمس بـ`atom._running=False` مباشرة (خمستها خزّنت حالتها رغم ذلك)، إدخال
4106 سجلّ لإثبات الإزاحة عند 4096 بالضبط، ومقارنة صيغة البحث القديمة
(فعليًا فشلت بمفتاح مختلف) بالجديدة (وجدت الحكم) على نفس السجلّ المخزَّن.

**الوصل (v5.5.0).** بعد الإصلاح، وُصلا فعليًا: خمس دوال حالة بـ`atom.py`
صارت سطرًا واحدًا يفوّض لـ`state_inputs.py`؛ كتلة فحوص الفتح الثلاثة
(~40 سطرًا) داخل `_on_built` صارت استدعاءً واحدًا لـ
`order_gates.run_open_gates()`. أُزيلت تبعًا لذلك: `_remember()`
ومقيّدها من `atom.py` (لا مستدعي لهما بعد التفويض)، والثوابت
`STAGE_PARENT`/`STAGE_MARGIN`/`STAGE_SNAPSHOT`/`SNAPSHOT_USABLE_STATUS`/
`AUTHORITY_FIELDS` (مستخدَمة فقط داخل الكتلة المزالة، ولها نظائر بـ
`order_gates.py`)، واستيراد `_neutral_pair_contract` (صار بلا استخدام).
**العدد الفعلي: 345 → 282 سطرًا** (وفّر 63، فعاد الهامش الحقيقي 68 سطرًا
بدل 5 — الذرّة كانت قريبة جدًا من حدّ 350 المخالَف مرّة سابقًا بتاريخها).

**أُثبت الوصل بلا تغيير سلوك، بثلاث طبقات مستقلّة:**
1. ملفّ اختبار الذرّة الـ11 سيناريو **بلا أي تعديل عليه** — نجح حرفيًا
   بنفس المخرجات قبل وبعد الوصل.
2. **محاكاة تفاضليّة خارج أدوات المشروع:** 9 سياقات (ترخيص كامل، قائمة
   بيضاء غائبة، مطابقة فاشلة، مرجع معطوب، تعرّض غير صالح، إيقاف عام،
   إيقاف حساب، بوّابة معطَّلة، ساعة غير متزامنة) × 10 أشكال أوامر (تغطّي
   كل تفرّع بالبوّابات الثلاث الموصولة) = 90 حالة، شُغِّلت على عمليتين
   منفصلتين تمامًا (كود قديم غير موصول / كود موصول)، وقورنت حرفيًا: كل
   حدث منشور، كل رباعية حاجز، وكل قاموس حالة داخلي. **90 من 90 مطابقة،
   صفر اختلاف** (توزّعت على 10 مخرجات مختلفة فعليًا — ليست حالة تافهة).
3. أُعيدت نفس المحاكاة الـ90 بعد التطبيق الفعلي على `atom.py` الحقيقي
   (لا نسخة مؤقّتة) — **صفر اختلاف عن خط الأساس** أيضًا.

**ملاحظة جانبيّة مسجَّلة بأمانة، خارج نطاق هذا البند:** أثناء التحقّق
اكتُشفت طبقة فحوص منفصلة عابرة للذرّات (`governance/checks/*.py`، 71
ملفًّا) لم تُشغَّل طوال هذه المراجعة كاملة. عيّنة من 6 ملفّات تخصّ 552
اختُبرت **على الكود الحقيقي قبل أي لمس**: 4 كانت أصلاً معطوبة أو فاشلة
لأسباب لا علاقة لها بهذا البند (بعضها يقرأ دالّة غير موجودة أصلًا بـ
`atom.py`)، والفحص الوحيد الناجح فعليًا (`check_execution_state_truth_
contract.py`) تحقّق مباشرة أنه ما زال يمرّ. لم تُمَسّ هذه الطبقة ولم
تُصلَح — خارج نطاق هذا البند تمامًا، تُذكَر للأمانة فقط.

**0 مخالفات، 0 تحذيرات بمدقّق الحوكمة الرسمي بعد كل خطوة.**

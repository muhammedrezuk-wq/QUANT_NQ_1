# Management Sender

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٧٥` |
| الاسم | Management Sender |
| المجلّد | `575_مرسل_الإدارة` |
| الإصدار | `2.3.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `execution.manage.command`
- `execution.gate.command`
- `emergency.halt`
- `risk.kill_switch.reset_requested`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `execution.manage.written`
- `execution.command.failed`

**إعداداتها:** `enabled` · `db_path` · `magic`

<!-- BITAQA_END -->

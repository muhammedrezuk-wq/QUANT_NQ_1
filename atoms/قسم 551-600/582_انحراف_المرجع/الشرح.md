# Reference Broker Divergence

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٨٢` |
| الاسم | Reference Broker Divergence |
| المجلّد | `582_انحراف_المرجع` |
| الإصدار | `2.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `feed.ctrader.tick`
- `feed.mt5.tick`
- `market.symbol_specs`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `execution.reference_divergence.state`

**إعداداتها:** `max_deviation_points` · `max_age_s` · `alignment_window_s` · `window_ticks` · `window_seconds` · `suspicious_repeats` · `expected_return_abs` · `suspicious_return_abs`

**٢.٠.٠ — ورقة المالك:** كاشف اختلاف حركة بين تياري cTrader و MT5، لا حكم تلاعب. أربع طبقات CLOCK/LEVEL/MOVE/BEHAVIOR. مراقبة فقط — لا إيقاف صفقة من هذه الذرّة.

<!-- BITAQA_END -->

# التاريخ — 582_انحراف_المرجع

**الذرّة:** `٥٨٢` · **الإصدار:** `2.0.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٩-٠٢ | `2.0.0` | ورقة المالك: كاشف اختلاف حركة لا حكم تلاعب. `timestamp_gap_s` لا يُطرح عبر مجالين (SendingTime UTC مقابل ساعة وسيط UTC+3 ≠ تأخير ٣ ساعات) — تُعلَن `broker_clock_offset_s` ويُقاس `receipt_gap_s` من الاستقبال. أربع طبقات CLOCK/LEVEL/MOVE/BEHAVIOR. تصنيف NORMAL · LEVEL_OFFSET_ONLY · EXPECTED_DIVERGENCE · SUSPICIOUS_DIVERGENCE · CLOCK_INVALID · STALE · INSUFFICIENT_DATA. التقادم من عمر الاستقبال (`received_at`/سمع محلي) لا من `clock.now()-SendingTime`. `exchange_timestamp=None` يسقط إلى `broker_timestamp`. الإصدار الأول مراقبة فقط (`observe_only`) — لا BLOCK/HALT. `status=SYNCED` للعيّنات القابلة للمقارنة حتى لا تُوقف ٥٧٨ تعرّضًا من تصنيف مراقبة. لا يُمسّ ٣٥٩ ولا غير ٥٨٢ |
| ٢٠٢٦-٠٨-١٨ | `1.4.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

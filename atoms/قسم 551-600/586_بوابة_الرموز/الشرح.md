# Durable Symbol Resolution Gate

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٨٦` |
| الاسم | Durable Symbol Resolution Gate |
| المجلّد | `586_بوابة_الرموز` |
| الإصدار | `2.0.2` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `execution.order.requested`
- `symbol.resolve.result`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `symbol.resolve.requested`
- `storage.symbol.resolve_requested`
- `execution.order.resolved`
- `execution.order.rejected`
- `symbol.resolve.orphaned`

**إعداداتها:** `resolution_timeout_s`

<!-- BITAQA_END -->

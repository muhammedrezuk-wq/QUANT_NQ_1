# Execution Snapshot With Completeness

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٨٣` |
| الاسم | Execution Snapshot With Completeness |
| المجلّد | `583_لقطة_التنفيذ` |
| الإصدار | `3.0.2` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_SECOND`
- `perpetual.target.state`
- `risk.asset_ledger.state`
- `asset.portfolio.state`
- `feed.mt5.tick`
- `market.symbol_specs`
- `dial.profile.state`
- `platform.account.state`

**تُرسِل (من `manifest.yaml`):**
- `execution.snapshot.state`

**إعداداتها:** `ledger_max_age_s` · `portfolio_max_age_s` · `price_max_age_s` · `specs_max_age_s` · `dial_max_age_s`

<!-- BITAQA_END -->

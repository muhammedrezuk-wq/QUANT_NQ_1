# Order Builder

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٥١` |
| الاسم | Order Builder |
| المجلّد | `551_باني_الأمر` |
| الإصدار | `4.3.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `risk.validation.completed`
- `risk.position_size.state`
- `risk.position_size.rejected`
- `platform.account.state`

**تُرسِل (من `manifest.yaml`):**
- `execution.order.built`
- `execution.desired.state`
- `execution.order.skipped`

**إعداداتها:** `reward_risk` · `magic`

<!-- BITAQA_END -->

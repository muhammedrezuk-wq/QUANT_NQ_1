# التاريخ — 800_النسخ_الاحتياطي

**الذرّة:** `٨٠٠` · **الإصدار:** `2.2.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.2.1` | تصحيح دلالة الصحّة بقاعدة المالك («جاهز بلا عمل بعد = سليم برسالة صادقة»): «لم تُنشأ نسخة بعد» مع جدولة مسلّحة وبلا خطأ صار `HEALTHY` «جاهز — بانتظار أول موعد نسخ»، و«تخطّي اللحاق ضمن النافذة» (نسخة سابقة قائمة) صار `HEALTHY` بذكر السبب. الأعطال الفعلية (`last_error`) تبقى متعثّرة. حارس `test_health_states` حُدّث للسلّم الجديد (٤/٤). |
| ٢٠٢٦-٠٨-١٨ | `2.2.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 2.2.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- «جاهز — نسخة سابقة ضمن النافذة (%s)» → `READY - previous backup within window (%s)`.
- رسالة الجاهزية: «جاهز — بانتظار أول موعد نسخ احتياطي» → `READY_AWAITING_FIRST_BACKUP_RUN`.
- التعليق المحذوف (قاعدة الصدق بأمر المالك) موثّق أصلًا في بند 2.2.1 أعلاه.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 60` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

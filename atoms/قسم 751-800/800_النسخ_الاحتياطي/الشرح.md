# Backup Manager

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٨٠٠` |
| الاسم | Backup Manager |
| المجلّد | `800_النسخ_الاحتياطي` |
| الإصدار | `2.2.2` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٦٠٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_DAY`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `tools.backup.completed`
- `tools.backup.failed`

**إعداداتها:** `backup_dir` · `source_dirs` · `keep_last_n` · `interval_days`

<!-- BITAQA_END -->

# التاريخ — 517_نتيجة_الصفقة

## 4.0.2 — 2026-08-20
- **تنظيف لغة الكود (م3/ملف4 — عربي داخل atom.py):** رسالة جاهزية الصحّة: «جاهز — بانتظار أول نتيجة صفقة محقّقة» ← `READY_AWAITING_FIRST_TRADE_OUTCOME` (الذيل `| outcomes=0` كما هو). حُدِّث فحص الاختبار `startswith`. صفر تغيير في المنطق.

**الذرّة:** `٥١٧` · **الإصدار:** `4.0.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `4.0.1` | تصحيح دلالة الصحّة بأمر المالك («ما بدي أشوف شي متعثّر أبدًا — مو كذب، حقيقة»): `NO_TRADES_YET` كان `DEGRADED` رغم أن النظام كلّه لم يفتح أي صفقة بعد — صفر نتائج هو الواقع لا عجز. صارت `HEALTHY` برسالة «جاهز — بانتظار أول نتيجة صفقة محقّقة \| outcomes=0» مع بقاء العدّادات بالتفاصيل. حُدّث الحارس `test_no_trades_degraded`→`test_no_trades_ready` ليثبّت الدلالة الجديدة. `NOT_STARTED`/`DURABLE_CONSUMER_UNAVAILABLE`/`OUTCOME_PARTIAL_OR_INCOMPLETE` تبقى عجزًا فعليًّا. صفر تغيير سلوكي آخر. |
| ٢٠٢٦-٠٨-١٨ | `4.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

## 4.1.0 — 2026-08-21
**حزمة ت (ت١) — ضمن ختم البند 22.** زوج هوية القرار (`decision_id` · `gate_request_id`) يركب النتيجة إلى **تقرير الخسارة**، مدموجًا دمجًا دائمًا مع بقيّة حالة الصفقة.
- الحقلان أُضيفا لقائمة حقول الحالة المدموجة، فينجوان من الدمج التدريجيّ للأحداث.
- الغائب يمرّ `None` **معلَنًا** (`identity_missing` + `identity_warnings`) — لا اختراع.
- عدّاد `identity_incomplete` ظاهر بتفاصيل الصحّة.
- **مقيس:** 4/4 اختبارات خضراء، والذرّة **حيّة 4.1.0 سليمة** (`READY_AWAITING_FIRST_TRADE_OUTCOME`).

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

# Reference Feed Health

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٢١` |
| الاسم | Reference Feed Health |
| المجلّد | `521_صحة_المرجع` |
| الإصدار | `2.3.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `feed.ctrader.tick`
- `feed.mt5.tick`
- `market.reference`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `reference.health.state`

**إعداداتها:** `symbols` · `max_data_age_s` · `fallback_max_age_s` · `max_sane_spread` · `max_tick_jump` · `min_dwell_s`

<!-- BITAQA_END -->

# Session Limits

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٠٦` |
| الاسم | Session Limits |
| المجلّد | `506_حدود_الجلسة` |
| الإصدار | `1.4.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `analysis.session.state`
- `platform.account.state`
- `risk.loss_reported`
- `platform.trade_event`
- `SYS_DAY`
- `risk.kill_switch.reset_requested`

**تُرسِل (من `manifest.yaml`):**
- `risk.session_limits.state`
- `risk.halt.requested`

**إعداداتها:** `max_session_loss_pct` · `max_session_trades` · `max_equity_drawdown_pct`

<!-- BITAQA_END -->

# التاريخ — 506_حدود_الجلسة

**الذرّة:** `٥٠٦` · **الإصدار:** `1.4.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٨ | `1.4.0` | تصحيح رقم إصدار فقط — الكود كان منذ جلسة الأحد الموازية يقرأ `equity` عبر `bind_truth`/`financial_truth` (`_on_equity` + إعلان `FINANCIAL_TRUTH_MISSING` عند الغياب)، بالضبط كما وثّقه تقرير تلك الجلسة، لكن رقم الإصدار بقي `1.3.0` بالمانيفست و`atom.py` معًا — لم يُرفَع رغم التغيير الوظيفي الحقيقي. **صفر تغيير سلوك بهذا التعديل** — الرقم فقط صار يطابق الكود الفعلي |
| ٢٠٢٦-٠٨-١٨ | `1.3.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

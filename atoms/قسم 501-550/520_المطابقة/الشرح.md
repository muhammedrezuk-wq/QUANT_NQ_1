# Position Reconciliation

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٢٠` |
| الاسم | Position Reconciliation |
| المجلّد | `520_المطابقة` |
| الإصدار | `3.5.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `execution.desired.state`
- `platform.positions.state`
- `execution.command.ack`
- `execution.reconcile.requested`
- `market_data.feed_recovered`
- `platform.account.state`

**تُرسِل (من `manifest.yaml`):**
- `execution.reconcile.state`

**إعداداتها:** `state_path`

<!-- BITAQA_END -->

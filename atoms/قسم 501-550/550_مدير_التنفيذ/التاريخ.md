# التاريخ — 550_مدير_التنفيذ

## 2.1.2 — 2026-08-20
- **تنظيف لغة الكود (م3/ملف4 — عربي داخل atom.py):** رسالة جاهزية الصحّة: «جاهز — بانتظار أول قرار نهائي أو أمر» ← `READY_AWAITING_FIRST_FINAL_DECISION_OR_ORDER` (الذيل `| finalized=0 queued=0` كما هو). صفر تغيير في المنطق.

**الذرّة:** `٥٥٠` · **الإصدار:** `2.1.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.1.1` | تصحيح دلالة الصحّة بأمر المالك («ما بدي أشوف شي متعثّر أبدًا — مو كذب، حقيقة»): `NO_INPUT_YET` كان `DEGRADED` رغم أن النظام لم يُنهِ أي قرار ولم يبنِ أي أمر بعد — سلسلة التنفيذ كلّها بلا عمل بحكم الواقع، لا عجز في الذرّة نفسها. صارت `HEALTHY` برسالة «جاهز — بانتظار أول قرار نهائي أو أمر \| finalized=0 queued=0» مع بقاء كل العدّادات بالتفاصيل. `NOT_STARTED` وخطأ الاستعادة/فقد الهويّة تبقى عجزًا فعليًّا. صفر تغيير سلوكي آخر. |
| ٢٠٢٦-٠٨-١٩ | `2.1.0` | **بالتزامن مع إصلاح `551` (راجع تاريخه):** اشتراك جديد على `execution.order.skipped` + عدّاد `order_skipped` بالحالة الموحّدة، وتفصيل تجميعيّ جديد `reject_reasons`/`skip_reasons` (من `execution.order.rejected` و`execution.order.skipped` معًا) — يُخزَّن ويُستعاد باللقطة الحيّة كمان. **اكتُشف بالمناسبة:** `execution.order.preview` اللي كانت الواجهة والفحوص تفترضها **غير موجودة إطلاقًا بكود `552` الحالي** — كل رفض من `552` (بما فيها البوّابة المقفولة `reason=disabled`) يمرّ فعليًّا عبر `execution.order.rejected` وحده. هذه الذرّة لم تكن تشترك أصلًا على `execution.order.preview` فلا تأثير عليها من هالجزء؛ التصحيح بالواجهة وملفّي الشرح `550`/`552` (راجع `سياق/٩٠`). |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

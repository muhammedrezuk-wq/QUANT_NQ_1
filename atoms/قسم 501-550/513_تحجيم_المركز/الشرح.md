# Position Sizing

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥١٣` |
| الاسم | Position Sizing |
| المجلّد | `513_تحجيم_المركز` |
| الإصدار | `4.3.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.tick.validated`
- `platform.account.state`
- `market.symbol_specs`
- `market.ctrader.symbol_specs`
- `risk.structure_stop.state`

**تُرسِل (من `manifest.yaml`):**
- `risk.position_size.state`
- `risk.position_size.rejected`

**إعداداتها:** `risk_per_trade_pct` · `default_stop_pct` · `min_lot` · `max_lot` · `lot_step` · `specs_max_age_s`

<!-- BITAQA_END -->

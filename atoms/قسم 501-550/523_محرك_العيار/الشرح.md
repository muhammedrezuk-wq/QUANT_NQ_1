# Dial Engine

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٥٢٣` |
| الاسم | Dial Engine |
| المجلّد | `523_محرك_العيار` |
| الإصدار | `2.3.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `risk.asset_ledger.state`
- `dial.command`
- `platform.account.state`
- `SYS_SECOND`
- `decision.settings.state`

**تُرسِل (من `manifest.yaml`):**
- `dial.profile.state`
- `horizon.profile.state`

**إعداداتها:** `dials` · `default_dial` · `horizon_min_s` · `horizon_max_s` · `horizon_shape` · `filter_min` · `filter_max` · `stop_min_frac` · `stop_max_frac` · `cadence_fast_s` · `cadence_slow_s`

<!-- BITAQA_END -->

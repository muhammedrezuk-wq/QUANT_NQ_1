# Candlestick Analyzer

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `١٥٦` |
| الاسم | Candlestick Analyzer |
| المجلّد | `156_الشموع` |
| الإصدار | `2.4.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market_data.candle_closed`
- `market.tick.validated`
- `analysis.settings.command`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `analysis.candle.state`
- `analysis.setting.changed`

**إعداداتها:** `doji_body_ratio` · `marubozu_body_ratio` · `pin_wick_ratio` · `pin_body_ratio`

<!-- BITAQA_END -->

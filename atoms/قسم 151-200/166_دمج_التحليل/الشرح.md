# Analysis Fusion

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `١٦٦` |
| الاسم | Analysis Fusion |
| المجلّد | `166_دمج_التحليل` |
| الإصدار | `2.7.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٦٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `analysis.cycle.collected`
- `decision.settings.command`

**تُرسِل (من `manifest.yaml`):**
- `analysis.section.live`
- `analysis.raw.completed`
- `analysis.fast.state`
- `analysis.slow.state`
- `decision.settings.state`

**إعداداتها:** `section_weight` · `agree_threshold` · `live_stale_after_s` · `fast_weight` · `slow_weight` · `fast_required_depth` · `slow_required_depth`

<!-- BITAQA_END -->

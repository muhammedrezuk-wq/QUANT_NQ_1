# cTrader FIX Price Feed

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦٢٢` |
| الاسم | cTrader FIX Price Feed |
| المجلّد | `622_تغذية_FIX_سي_تريدر` |
| الإصدار | `1.4.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `feed.ctrader.tick`
- `market.ctrader.symbol_specs`
- `market.depth`
- `feed.ctrader.heartbeat`
- `feed.ctrader.state`

**إعداداتها:** `host` · `port` · `use_tls` · `sender_comp_id` · `target_comp_id` · `sub_id` · `username` · `identity_account_id` · `broker` · `password_secret_key` · `symbols` · `market_depth` · `heartbeat_s` · `poll_interval_s` · `reconnect_backoff_s` · `live_max_backlog_bytes` · `tick_stale_after_s` · `depth_stale_after_s` · `specs_stale_after_s` · `dead_after_s`

<!-- BITAQA_END -->

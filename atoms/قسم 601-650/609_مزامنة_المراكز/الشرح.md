# Position Sync

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦٠٩` |
| الاسم | Position Sync |
| المجلّد | `609_مزامنة_المراكز` |
| الإصدار | `5.0.2` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `platform.account.state`
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `platform.positions.state`
- `platform.position.appeared`
- `platform.position.vanished`
- `platform.positions.schema_state`

**إعداداتها:** `db_path` · `table_name` · `poll_interval_s` · `stale_after_s`

<!-- BITAQA_END -->

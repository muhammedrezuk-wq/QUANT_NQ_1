# التاريخ — 616_جسر_الأخبار

**الذرّة:** `٦١٦` · **الإصدار:** `1.4.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٧ | `1.4.0` | **بند ٢١/٢٧ بمراجعة ٢٧ ذرّة: نفس اسم الحدث بشكلين متضاربين بين ٦١٥ و٦١٦.** التزامن نفسه مقصود (سجلّ `615`: «مسار ثانٍ موازٍ» بقرار المالك)؛ البند يخصّ تضارب شكل الحمولة فقط. علّتان حقيقيّتان: (١) `impact_level` هنا يرسل `"UNKNOWN"` دائمًا (أمر مالك سابق، ثابت `IMPACT_UNKNOWN`) بينما `615` كان يرسل `None` — بقي هذا هو المرجع، `615` صار يطابقه. (٢) `id` كان رقم صفّ خام من قاعدة كل ذرّة المستقلّة — صفّ `615` رقم ١٧ وصفّ `616` رقم ١٧ خبران مختلفان يتشاركان رقمًا بالمصادفة، والمستهلك الوحيد المؤكَّد (`108`) يستعمله كمفتاح إزالة تكرار. صار `id` المنشور `"616:<الرقم الخام>"`. **`written_at`** أيضًا صار يُرسَل دائمًا (كان يُسقَط إن غاب) ليتّفق مع عرف `615` بلا تعارض جديد — لا مستهلك يقرأه اليوم فالتغيير آمن. **`symbols` لم يتغيّر** — عرف هذه الذرّة («عمود غائب = حقل غائب»، بند ٢٢ حزمة أ أدناه) تأكَّد أنّه الأكثر تعمُّدًا بين الاثنين، و`615` طابقته بدل العكس. **أُثبت تجريبيًا:** تراجعٌ مؤقّت على `id` بكلتا الذرّتين أنتج فعليًّا `duplicate=1` بذرّة `108` (خبرا `615`/`616` رقم ١ تصادما وسقط الثاني بصمت) — اختبار طرف لطرف بكود `108`/`615`/`616` الحقيقي. 28/28 اختبارًا خضراء عبر الثلاث ذرّات، 0 مخالفات (بعد تنظيف اقتباس عربي مباشر أدخلتُه أنا بتعليقات `atom.py` — م3/ملف4). |
| ٢٠٢٦-٠٨-٢٥ | `1.3.0` | **وقت السحب ما عاد يتنكّر ختمًا للحدث — بختم `NQ`:** كان `stamp = published_at if not None else written_at` ثمّ يُنشر باسم `timestamp`. و`written_at` هو **لحظة سحبنا نحن**، لا لحظة الخبر. ⇒ خبر عمره ثلاثة أسابيع يصل النظام مختومًا «الآن»، وحاجز الخبر القويّ سؤالُه بالضبط «هل وقع خلال ±١٥ دقيقة؟». **المقيس على قاعدة الجسر: ٢٠ صفًّا من ٢١٤ بلا وقت نشر، والأخبار تمتدّ من ٢٠٢٦-٠٨-٠٤ إلى ٢٠٢٦-٠٨-٢٤.** صار `timestamp` يُوضَع **فقط** حين يوجد وقت نشر حقيقيّ، و`written_at` يمرّ **باسمه الصريح** فلا يضيع ولا يكذب. **والتشخيص وصل من جلسة بوت الأخبار الموازية وقُوبل بكودنا فثبت** — الكاتب عندهم يترك الحقل فارغًا بأمانة، والاستبدال كان عندنا. |
| ٢٠٢٦-٠٨-٢٥ | `1.2.0` | **ترتيب قراءة التقويم انقلب — بختم `NQ`:** كان `ORDER BY scheduled_at` أي **أقدم** `batch_limit` صفًّا. **والمقيس على قاعدة الجسر الحيّة:** جدول `calendar` فيه **٨١٥ صفًّا · ٧٣٦ ماضٍ · ٧٩ مستقبل**، و**أقدم ٥٠٠ صفّ فيها صفر حدث مستقبليّ**. فكان `616` يبثّ ٥٠٠ حدثًا ماضيًا كلّها، و`109` يشطبها عن آخرها (`tracked=0 published=1` مقيسًا)، **فلا يصل النظامَ حدثٌ اقتصاديّ واحد**. صار `ORDER BY scheduled_at DESC` فيأخذ الأحدث، وفيها كل المستقبليّ دائمًا. **ولا تُقرأ ساعة هنا** — الترتيب وحده يحلّها، والقاعدة ١٣ سليمة. **صفر تغيير آخر:** استعلام الأخبار ومؤشّره وأعمدته كما هي. |
| ٢٠٢٦-٠٨-٢٠ | `1.1.1` | **سدّ فجوة الرموز بختم NQ بند 22 حزمة أ — ق٧:** استعلام الأخبار يُبنى من مخطط الجدول وقت التشغيل (`PRAGMA table_info`) بأعمدة مسمّاة: `symbols` و`impact_level` يُمرَّران بالحمولة **إن وُجدا فعلًا** بالجدول، وغياب العمود سقوطٌ آمن (حقل غائب لا انهيار ولا قيمة مخترعة). قائمة الرموز تُفكّ بفواصل على عرف 615 (`UNKNOWN`/فارغ = لا ادّعاء، فيُحذف الحقل لا يُرسَل فارغًا). بلا عمود `id` لا قراءة (لا ترقيم آمن بدونه). |
| ٢٠٢٦-٠٨-١٨ | `1.1.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 60` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

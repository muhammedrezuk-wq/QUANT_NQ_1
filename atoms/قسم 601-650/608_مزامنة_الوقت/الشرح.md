# Time Sync

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦٠٨` |
| الاسم | Time Sync |
| المجلّد | `608_مزامنة_الوقت` |
| الإصدار | `2.3.0` |
| حرجة؟ | `true` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- لا شيء

**تُرسِل (من `manifest.yaml`):**
- `time.ntp.samples.state`
- `time.ntp.drift`
- `time.utc.stale`

**إعداداتها:** `reference_servers` · `sync_interval_s` · `query_timeout_s` · `drift_alert_s` · `stale_after_s` · `max_accepted_offset_s` · `max_sample_deviation_s` · `min_samples`

<!-- BITAQA_END -->

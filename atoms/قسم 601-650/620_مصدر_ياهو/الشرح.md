# Yahoo Finance Source

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦٢٠` |
| الاسم | Yahoo Finance Source |
| المجلّد | `620_مصدر_ياهو` |
| الإصدار | `1.2.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `market.reference`

**إعداداتها:** `symbols` · `base_url` · `poll_interval_s` · `request_timeout_s` · `max_consecutive_errors`

<!-- BITAQA_END -->

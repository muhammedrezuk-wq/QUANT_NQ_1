# Market Feed Hub

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦١٣` |
| الاسم | Market Feed Hub |
| المجلّد | `613_تغذية_السوق` |
| الإصدار | `2.5.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `feed.mt5.tick`
- `feed.ctrader.tick`
- `kernel.clock.heartbeat`

**تُرسِل (من `manifest.yaml`):**
- `market.tick`
- `market.volume`
- `market.feed.provider_down`
- `market.feed.provider_recovered`

**إعداداتها:** `routes` · `provider_timeout_s` · `max_input_silence_seconds` · `preferred_provider`

<!-- BITAQA_END -->

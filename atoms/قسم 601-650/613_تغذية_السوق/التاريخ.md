# التاريخ — 613_تغذية_السوق

**الذرّة:** `٦١٣` · **الإصدار:** `2.3.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `2.3.0` | **إصلاح حي مقيس (يخفي تدهور المسار):** `health_check()` كانت تراقب ساعة صمت واحدة مشتركة (`self._last_input_at`) تتحدّث من MT5 أو cTrader سيّان — فتبقى "سليمة" ما دام مصدر واحد فقط حيّ، حتى لو مات المصدر الآخر كلياً. مقيس حياً: أثناء انقطاع MT5 (طرفيته مطفأة ٢٨.٧ ساعة) كانت ٦١٣ تقول `healthy — forwarding 1 symbols via 2 routes` بلا أي إشارة. المفارقة: العلم `down` لكل مصدر كان محسوباً فعلاً بدالة `_on_heartbeat` (بنفس `provider_timeout_s` الموجود أصلاً) لكن مدفوناً بـ`details["providers"]` ولا يوصل لحالة الفحص العليا. الإصلاح: `health_check()` تفحص الآن أعلام `down` لكل مصدر وتسمّي المصدر الميت صراحة بالرسالة العليا (`PROVIDER_DOWN: MT5`) — بلا أي رقم إعداد جديد مخترَع، استعملت العتبة الموجودة أصلاً فقط. حارس جديد `test_health_check_surfaces_single_dead_provider` بُني أولاً وسقط على الكود القديم، ثم صلحته، ثم مرّ (٦/٦ اختبارات). **ملاحظة جانبية غير مُصلَحة عمداً:** اكتُشف أثناء التشخيص أن `core/api/app.py::_serialize_atom()` يحذف حقل `details` من استجابة كل الذرات قبل وصولها للوحة — يعني أي تفصيل إضافي غير الرسالة العليا لن يظهر أبداً بأي حال. هذا عطل بمستوى النواة (core/) ولا يُلمس بلا إذنك الصريح وإعادة ختم — سجّلته فقط. |
| ٢٠٢٦-٠٨-١٨ | `2.2.1` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-18 03:30`

## 2.4.0 — 2026-08-23
- المسار (ب) من ورقة X (أمر تبنّي المالك ٢٠٢٦-٠٨-٢٣): إضافة "sequence" إلى `_IDENTITY_KEYS` فتُمرَّر هوية التِكّة المرقّمة كما أرسلها المصدر (622 ينشرها منذ 1.0.6). تقوية مستقلة للمسار (أ) — لا تلغيه.

## 2.4.1 — 2026-08-23
- امتداد المسار (ب) نفسه: إضافة "tick_id" إلى `_IDENTITY_KEYS` — القناة الأولى في سلسلة هوية المحوّل القانوني. بدونه تُسقط التغذيةُ هويةَ أي مصدر يختم tick_id (مثل تغذية الإعادة 625) فتقع الهوية على event_id العشوائي وتُفقد حتميّة دورات المحاكاة. لا تغيير سلوكي آخر.

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 60` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

## 2.5.0 — 2026-08-27 (found during a full 304-atom audit, not assumed)

**Same family of bug as v2.3.0 above -- another way for a dead feed to
read as HEALTHY.** `self._last_input_at` (the clock `health_check()`'s
`INPUT_STARVED` check reads) was touched at the very TOP of
`_on_source_tick`, before either validation block. A feed sending
nothing but malformed packets (bad bid/ask, missing timestamp, whatever)
refreshed the clock on every single arrival even though every one of
them got dropped -- so `INPUT_STARVED` could never trip as long as
*something*, however garbled, kept arriving on the wire. Combined with
v2.3.0's own finding that `core/api/app.py` strips `details` from every
atom's health response before it reaches the dashboard, this meant the
one channel a human operator actually sees (top-level `state`/`message`)
would keep saying HEALTHY through a 100%-drop-rate feed.

**Fix:** moved `self._last_input_at = time.time()` past both validation
blocks -- it now advances only on a tick that actually parses as usable
(valid bid/ask/timestamp), matching what `INPUT_STARVED` is meant to
measure ("is the feed alive and useful", not "is *anything* arriving on
the wire"). A tick suppressed later for secondary-provider deduplication
still counts as valid input (the feed proved itself alive; the atom just
chose not to forward that one) -- only genuinely malformed packets no
longer count.

New test `test_garbage_only_feed_does_not_fake_freshness`: pins
`_last_input_at` far in the past, feeds five malformed packets, asserts
`health_check()` reports `DEGRADED`/`INPUT_STARVED`. Verified empirically
against a disposable reverted copy of the old ordering first -- it fails
there with `HealthState.HEALTHY` (the exact bug), and passes clean
against every other one of the 6 pre-existing tests, isolating that this
test catches exactly this bug. 7/7 tests, validator 0 violations.

# التاريخ — 601_كاتب_جسر_الدماغ

**الذرّة:** `٦٠١` · **الإصدار:** `4.0.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `4.0.1` | **سدّ فجوة الأمر البائت الصامت** (كُشفت أثناء بناء إكسبرت v3.00): الإكسبرت يختم الأمر المتجاوز عمره `EXPIRED` وقارئ النتائج هنا كان يعرف `DONE/FAILED/CANCELLED` فقط — فيمرّ موت الأمر بلا نجاح ولا فشل ولا يعلم النظام. صار `EXPIRED` فشلًا معلنًا (`execution.command.failed` بسببه)، وأي حالة نتيجة مجهولة تُعلن فشلًا باسمها — فشل-مغلق: لا موت أمرٍ يمرّ صامتًا. |
| ٢٠٢٦-٠٨-١٨ | `4.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`
## 4.0.2 — 2026-08-20
- تنظيف مادة اللغة (دستور الذرّة): أزيل كل حرف عربي من atom.py — التعليقات العربية حُذفت والنصوص العربية استُبدلت برموز إنكليزية. صفر تغيير منطقي، الاختبارات خضراء.
- التعليقات المحذوفة (فجوة EXPIRED الصامتة + الفشل-المغلق لحالة نتيجة مجهولة) موثّقة أصلًا في بند 4.0.1 أعلاه — لا معلومة ضاعت.

## 4.1.0 — 2026-08-21
**حزمة ت (ت١) — ضمن ختم البند 22.** خيط الهوية يعبر الجسر **داخل `params_json`** — وهو عمود بيانات وصفيّة **داخليّ** يملؤه هذا الكاتب أصلًا بكائن JSON اختياريّ المفاتيح.
- **شكل الجدول الخارجيّ المتفق عليه لم يُمَسّ**: لا عمود جديد، ولا مفتاح يقرأه الإكسبرت تغيّر — والإكسبرت يتجاهل ما لا يبحث عنه.
- أربعة حقول تُحمَل: `decision_id` · `gate_request_id` · `parent_decision_id` · `owner_command_id`.
- قراءة `params_json` من **صفوفنا نحن** عند تحصيل النتائج تُعيد الهوية لأحداث النتيجة بعد أيّ إقلاع — لا ذاكرة طيّارة.
- غياب الهوية **لا يحجب** الأمر هنا (الحجب ملك 552 حصرًا) بل يُعَدّ ويُعلَن (`identity_incomplete`).
- **مقيس:** 6/6 اختبارات خضراء، والذرّة **حيّة 4.1.0 سليمة** (`bridge writable`).

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 60` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

## 2026-08-27 — إصلاح اختبار يسمّم نفسه (لا تغيير بمنطق atom.py)

`test_identity_thread_rides_bridge_row_and_results` كان يفشل بثبات (لا تذبذب) بعد أن صار v4.2.0
(٢٠٢٦-٠٨-٢٥، غير موثّق بهذا الملف حين أُضيف) يُخزّن مؤشّر النتائج بملفّ **عالميّ ثابت**
`_CURSOR_DB = "var/store/bridge_cursor_601.db"` — خارج عزل `tempfile.TemporaryDirectory()` الذي
تستخدمه قاعدة الجسر نفسها بكل اختبار. تشغيلة سابقة للاختبار تركت الملف الحقيقيّ بالقيمة
`(done_at=1000.0, id=1)` — وهي **بالضبط** ما تنتجه قاعدة مؤقّتة جديدة (id يبدأ ١ دائماً) بنفس
`done_at` الذي يكتبه الاختبار — فقرأ التشغيل التالي مؤشّراً "من حياة سابقة" يطابق صفّه الجديد
حرفياً فرفضه (`done_at>? OR (done_at=? AND id>?)` كلاهما كاذب).
**لم يُلمَس مسار الإنتاج بـatom.py** (يبقى `var/store/bridge_cursor_601.db` الثابت عمداً — لا
علاقة له بـ`db_path` المُهيّأ من الإعداد، الذي يشير بالإنتاج لمجلّد MT5 الخارجيّ، لا مجلّد
المشروع). عُزل الاختبار وحده (`_mod._CURSOR_DB` يُشار مؤقّتاً لمسار داخل tempdir الاختبار نفسه)،
وحُذف الملفّ المسمَّم المتروك بـ`var/store/`. أُعيد التشغيل ٤ مرّات متتالية للتأكّد أنه ثبات حقيقيّ
لا صدفة: **6/6 في كل مرّة.**

**ملاحظة منفصلة، غير مُصلَحة اليوم:** المدقّق يُظهر مخالفتين سابقتين لهذا التاريخ لم ألمسهما —
حجم atom.py (٣٨٩ > ٣٥٠ سطراً فعلياً) وعربي داخل الكود — لأن أولوية اليوم كانت تحديداً إصلاح
فشل الاختبار المكتشَف حديثاً، لا جولة تدقيق كاملة لهذه الذرّة. موثَّقة هنا كي لا تُنسى.

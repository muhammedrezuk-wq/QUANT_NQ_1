# Trade Events Reader

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦١١` |
| الاسم | Trade Events Reader |
| المجلّد | `611_قارئ_الصفقات` |
| الإصدار | `4.0.3` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٥` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_SECOND`

**تُرسِل (من `manifest.yaml`):**
- `platform.trade_event`

**إعداداتها:** `db_path` · `table_name` · `poll_interval_s` · `batch_limit` · `max_age_s` · `cost_refresh_timeout_s`

<!-- BITAQA_END -->

# الذرة 626 — جسر التنفيذ الصوري

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦٢٦` |
| الاسم | Simulated Execution Bridge |
| المجلّد | `626_جسر_التنفيذ_الصوري` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `manual` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `trading.final_decision`
- `market.tick.validated`

**تُرسِل (من `manifest.yaml`):**
- `platform.trade_event`
- `sim.execution.state`

**إعداداتها:** `per_lot_commission` · `latency_ms`

<!-- BITAQA_END -->




يستقبل أوامر التنفيذ النهائية ويردّ بحشوات محسوبة من السبريد المسجّل — بلا أي اتصال وسيط.

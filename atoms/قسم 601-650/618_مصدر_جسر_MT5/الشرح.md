# MT5 Bridge Source

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦١٨` |
| الاسم | MT5 Bridge Source |
| المجلّد | `618_مصدر_جسر_MT5` |
| الإصدار | `4.4.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `SYS_SECOND`
- `platform.account.state`
- `SYS_10MS`

**تُرسِل (من `manifest.yaml`):**
- `feed.mt5.tick`
- `market.symbol_specs`

**إعداداتها:** `db_path` · `table_name` · `spec_table` · `spec_refresh_s` · `poll_interval_s` · `batch_limit` · `delete_consumed` · `max_age_s`

**٤.٤.٠ — باب UTC:** طابع وسيط صالح يُعتمد، الفاسد يُستبدل بوقت الاستلام. لا ±10800. الخام يبقى للتدقيق.

<!-- BITAQA_END -->

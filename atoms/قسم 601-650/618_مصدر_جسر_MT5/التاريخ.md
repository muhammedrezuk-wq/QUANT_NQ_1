# التاريخ — 618_مصدر_جسر_MT5

**الذرّة:** `٦١٨` · **الإصدار:** `4.4.0`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٩-٠٣ | `4.4.0` | جذر فرق الثلاث ساعات: باب تطبيع UTC. طابع وسيط صالح (±5ث من الاستلام) → `timestamp=broker`. طابع فاسد (UTC+3 ملبوس epoch وغيره) → `timestamp=received_at`، `exchange_timestamp=null`، الخام في `broker_timestamp_raw`. لا ±10800 ولا تخمين منطقة. اختبارات الورقة ١٥/١٥ (حالات ١٣–١٦، ١٨، ٢٢ + عقد الساعة). قياس حي BTCUSD/USTEC/XAUUSD على ويندوز بعد النقل |
| ٢٠٢٦-٠٨-١٩ | `4.0.1` | **تنفيذ اعتراض المالك المسجّل (٦١٧/٦١٨):** أُزيلت الأرضيّة المخترَعة `minimum: 0.01` من `poll_interval_s` في المخطّط (صارت `minimum: 0`) — القيمة حرّة بيد المالك بلا سقف مِن تحت. القيمة المضبوطة حاليًّا بقيت `0.01` عمدًا: هذه الذرّة تستعلم وتحذف من قاعدة SQLite يكتب فيها إكسبرت MT5 بنفس اللحظة، ورفع السرعة لأقصاها يحتاج قياس تنازع أقفال مع الكاتب أوّلًا — وحدة قادمة مستقلّة، لا رقم يُخترع اليوم. صفر تغيير سلوكي في الكود. |
| ٢٠٢٦-٠٨-١٨ | `4.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

- **حملة ٤٥٠–٩٠١ (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 60` في metadata — مشتق من إيقاع الاشتراكات، مؤقّت قابل لضبط المالك. صفر تغيير في الكود.

## 4.2.0 — 2026-08-24 — Patch قبل الختم
- قراءة ticks_v2 على SYS_10MS وتمرير broker مع المواصفات؛ إزالة poll loop.

## 4.3.0 — 2026-08-25 — nq seal
- The 2026-08-24 patch hung the ONLY tick pump on SYS_10MS — a pulse with ZERO publishers project-wide (found by the dashboard-model event inventory) — so the MT5 feed was structurally dead since that patch (the real root of MT5_TICK_FEED_STALE even with the terminal running). Restored the bridge-family paced poll task (622's pattern) as the pump; the SYS_10MS subscription stays as a harmless compatibility hook.

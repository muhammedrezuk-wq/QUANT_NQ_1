# التاريخ — 625_تغذية_الإعادة

## 1.0.0 — 2026-08-23
- الإنشاء بورقة X (البناء ٢، ختم المالك ٢٠٢٦-٠٨-٢٣): مصدر تِكّات المحاكاة — يقرأ `var/store/market_data.db` قراءةً فقط وينشر `feed.replay.tick` بحمولة 622 نفسها. هوية حتميّة بكل تِكّة (`tick_id` من رقم الصف) فإعادة نفس المقطع تعطي نفس دورات القرار. إقلاع يدوي + أمر `replay.session.start` صريح — لا يعمل في إقلاع الإنتاج أبدًا.
- تصحيح مدقّق: ثابت `FETCH_BATCH_ROWS` بدل الرقم السحري 500 (م9).

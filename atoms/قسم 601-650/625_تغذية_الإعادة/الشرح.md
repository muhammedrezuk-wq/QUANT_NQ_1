# الذرة 625 — تغذية الإعادة

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٣. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٦٢٥` |
| الاسم | Replay Feed Source |
| المجلّد | `625_تغذية_الإعادة` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `manual` |
| فحص الصحّة | كل `٣٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `replay.session.start`
- `replay.session.stop`

**تُرسِل (من `manifest.yaml`):**
- `feed.replay.tick`
- `replay.session.state`

**إعداداتها:** `db_path` · `symbols` · `limit` · `account_id` · `broker` · `tick_prefix` · `pace_seconds` · `progress_every`

<!-- BITAQA_END -->





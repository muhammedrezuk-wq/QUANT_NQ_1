# التاريخ — 251_مجمعات_السيولة

**الذرّة:** `٢٥١` · **الإصدار:** `1.2.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٨ | `1.1.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

## 1.2.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

## 1.2.1 — 2026-08-27 (بند ٢٦/٢٧ — فحصٌ من الصفر، لا نصّ تدقيق أصليّ متاح)

نصّ البند 26 الأصليّ غُيب من السياق (تلخيص طويل)، ولم يُعثَر عليه محفوظًا
بالمشروع (بحث شامل: لا بمجلّد `التدقيق/`، ولا بأي ملفّ آخر). بأمر المالك،
فُحصت الذرّة من الصفر بنفس صرامة بقيّة البنود، بلا ادّعاء أنّ ما يلي يطابق
نصًّا مفقودًا.

**ما فُحص وتبيّن أنه غير قابل للحدوث حاليًّا (لا علّة حيّة):**
1. `_emit()` تحوّل `score` بـ`int(score)` **بلا `try/except`** — خلافًا لكل
   تحويل رقميّ آخر بالملف (`_to_float` محروسة). لو وصل `score=None` أو
   نصًّا، هذا يسقط بخطأ خام. **تحقّقت من المنتِج الحقيقيّ الوحيد**
   لـ`structure.swing.state` (الذرّة 201 — بحث مشروع-كامل أكّد لا مُصدِر
   ثانٍ): `score = int(round(min(1.0, prominence) * 100))` بكل مسارات
   201 الثلاثة (نافذة ناقصة / لا إشارة / إشارة حقيقية) — عدد صحيح 0..100
   دومًا، لا `None` ولا نصّ إطلاقًا. **لا مسار حيّ يصل لهذا السقوط.**
   بحكم «لا تحقّق من سيناريو لا يحدث» (حدود النظام هنا داخليّة بين ذرّتين
   بنفس المشروع، لا حافّة خارجية) — **لم يُضَف حارس** دفاعيّ لا مبرَّر له.
2. إشارة `swing_high`/`swing_low` بسعر غائب (`metadata.price=None`) تسقط
   بصمت لـ`SIGNAL_NONE` بلا إنذار. **تحقّقت**: 201 لا يرسل إشارة حقيقية
   إلا مع سعر حقيقي مرفَق دومًا (`center["high"]`/`center["low"]` من
   بيانات شمعة فعلية) — **لا مسار حيّ لهذا أيضًا.**

**ملاحظة صادقة، بلا إصلاح (صفر مستهلك يتأثّر):** الذرّة لا تُمرّر
`status`/`quality` الواردين من 201 (تُصدر `"ok"`/`"good"` ثابتين دومًا،
حتى أثناء نافذة الإقلاع الناقصة بـ201 حيث يرسل `"insufficient"`/`"low"`)،
و`warnings` دومًا `[]`. **تحقّقت من كل المستهلكين الثلاثة** لـ
`liquidity.pool.state` (250، 252، 253 — بحث كامل بأكوادهم): لا واحد
منهم يقرأ `status`/`quality`/`warnings` إطلاقًا؛ يقرأون فقط
`id`/`confidence`/`signal`. فجوة أمانة بيانات حقيقية لكن **بلا أثر
تشغيليّ حاليّ** — لم تُصلَح، تُذكَر للشفافية فقط.

**الإصلاح الوحيد المنفَّذ:** اختبار جديد
`test_score_boundaries_no_crash_correct_confidence` يقفل طرفي مدى
المنتِج الحقيقيّ (score=0 مع إشارة حقيقية، score=100) — لم يكونا
مغطَّيَين بالاختبارات الستّة القائمة (التي تغطّي 67/55/20/90/none فقط).
7/7 اختبارات خضراء، 0 مخالفات بمدقّق الحوكمة الرسمي. **لا تغيير سلوكيّ —
نسخة اختبار فقط.**

# التاريخ — 300_مدير_الإحصاء

**الذرّة:** `٣٠٠` · **الإصدار:** `1.0.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٨ | `1.0.1` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 22:04`

## 1.1.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

## 1.2.0 — 2026-08-23
- مواءمة نسخة المانيفست مع `ATOM_VERSION` في الكود (كانت المانيفست متأخّرة — المدقّق الرسميّ: نسخة مزدوجة). لا تغيير في أي سطر سلوكي.


## 1.3.0 — 2026-08-23
**إقفال القسم 300 «الإحصاء» · ذرّات أولًا (أمر المالك).**

نفس علاج 200/250: لا مزيد من دورات التِكّة الفارغة — الدورة تُفتح من أول تسليم وحدة بهويتها هي وتكتمل **17/17** طبيعيًا (كل وحدات الإحصاء بمصادر حية — لا مستثنيات، والقائمة الفارغة معلنة كي يبقى القالب موحّدًا). حدث جديد `stats.units.state` (كل وحدة تعلن نفسها). المتأخر ظاهر بعدّاد. إعلان `stats.section.live` بالمانيفست (خرق القاعدة ١٧ — A §12.5). الاختبارات أُعيدت كتابتها: **5/5**.

## 2026-08-27 — حذف سكربت اختبار يتيم (لا تغيير بـatom.py)

`tests/test_tick_flow.py` (بختم «Owner 2026-08-22» — **يوم واحد قبل** إعادة كتابة هذه الذرّة أعلاه) كان يستدعي `mgr._on_tick(...)` و`mgr._cycles` — كلاهما غير موجود بالكود منذ 1.3.0. سكربت مستقل بلا دالّة `test_` واحدة (يُنفَّذ فور الاستيراد)، فكان يُسقِط تجميع pytest للذرّة كاملةً بـ`AttributeError`. مُستبدَل بالكامل بـ`tests/test_atom.py` الحالي (5/5 خضراء، مطابق للكود الفعليّ). حُذف — لا شيء فيه لا يغطّيه الاختبار الحاليّ.

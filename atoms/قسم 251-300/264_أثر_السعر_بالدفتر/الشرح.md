# Book Price Impact (Kyle Lambda)

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٢٦٤` |
| الاسم | Book Price Impact (Kyle Lambda) |
| المجلّد | `264_أثر_السعر_بالدفتر` |
| الإصدار | `1.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market_data.depth_updated`

**تُرسِل (من `manifest.yaml`):**
- `liquidity.price_impact.state`

**إعداداتها:** `weight` · `required_levels` · `impact_qty`

<!-- BITAQA_END -->

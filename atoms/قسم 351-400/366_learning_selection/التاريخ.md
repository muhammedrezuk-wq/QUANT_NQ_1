# التاريخ — 366_learning_selection

**الذرّة:** `٣٦٦` · **الإصدار:** `1.0.3`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٥ | `1.0.5` | **الملفّ كان مضغوطًا كاملًا وأُعيدت كتابته خامًا — بختم `NQ`:** إزاحة مسافة واحدة وفواصل `;`. صار خامًا بثوابت مسمّاة (`STAGE_DEFAULT` · `REASON_AWAITING`) وحكم `activation_stage` محفوظ حرفًا بحرف كما خُتم بـ`1.0.4`. **صفر تغيير في المنطق ولا في الحمولة ولا في رسائل الصحّة.** |
| ٢٠٢٦-٠٨-١٩ | `1.0.2` | تصحيح دلالة الصحّة بأمر المالك («ما بدي أشوف شي متعثّر أبدًا — مو كذب، حقيقة»): `selected=0` كان يُعلن `DEGRADED` رغم أن الذرّة تعمل سليمة ولم يصلها أي قرار مقارنة معتمَد بعد (لا صفقات مغلقة بالنظام) — جاهزية قبل أول عمل، لا عجز فعلي. صارت `HEALTHY` برسالة «جاهز — بانتظار أول نموذج معتمَد للاختيار | selected=0» مع بقاء العدّاد في التفاصيل. `NOT_STARTED` تبقى `UNHEALTHY`. صفر تغيير سلوكي آخر. |
| ٢٠٢٦-٠٨-١٩ | `1.0.1` | أضيف `name_ar: "اختيار نموذج التعلّم"` — كانت هذه من `٩` ذرّات (`360`-`368`) الاستثناء الوحيد بالمشروع بلا اسم عربي صريح، فطلع اسمها الإنكليزي الخام باللوحة. ترجمة مأخوذة حرفيًّا من `سياق/٩٧`. صفر تغيير سلوكي. |
| ٢٠٢٦-٠٨-١٨ | `1.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

## 1.0.3 — 2026-08-20
- تنظيف مادة اللغة — الكود يتكلم رموزًا إنكليزية واللوحة تترجم؛ لا تغيير منطق. استُبدلت رسالة الصحّة: «جاهز — بانتظار أول نموذج معتمَد للاختيار | selected=0» ← `READY_AWAITING_FIRST_APPROVED_MODEL | selected=0`

## 1.0.4 — 2026-08-23
- إضافة حقل `activation_stage` إلى حدث `learning.model.selected`: النموذج المُختار حديثًا يُعلن مرحلته، وظلًّا (shadow) بحكم التصميم ما لم يُورِّث المنبع مرحلة صريحة. كان الحقل مطلوبًا في عقد الاختبار (`test_learning_pipeline_end_to_end`) ولم يُنتج قطّ — قيد «شغل ارينا الناقص» في تقرير المشاكل ٢٠٢٦-٠٨-٢٣. لا تغيير في منطق الاختيار.

- **حملة ١–٤٤٩ · الدفعة ب (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع اشتراكات الذرّة، رقم كريم مؤقّت قابل لضبط المالك من اللوحة لاحقًا. صفر تغيير في الكود والسلوك.

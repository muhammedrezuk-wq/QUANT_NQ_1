# التاريخ — 400_مدير_الاستراتيجيات

**الذرّة:** `٤٠٠` · **الإصدار:** `1.2.3`

> **٢٠٢٦-٠٨-١٩ · `1.2.2` — «المبكّر» صنف ثالث صادق:** بعد امتصاص الصدى بقي `new_late`
> يتسلّق بالمئات: الوحدات المستمرة تبثّ بهوية الشمعة **الجارية** والمدير يفتح دورتها عند
> **الإغلاق** — بثّ استباقي بالتصميم لا تأخّر. صار: دورة لم تُفتح قط → `early` (مجرى،
> لا يمسّ الصحّة)؛ دورة أُغلقت بلا مساهمة الوحدة → `late` حقيقي يُعدّ. الحارس حُدّث
> بالحالات الثلاث (صدى/مبكّر/متأخّر حقيقي).

> **٢٠٢٦-٠٨-١٩ · `1.2.1` — امتصاص الصدى:** وحدات تبثّ حالتها كل ثانية بعد إغلاق دورتها
> (408/412 — `new_late≈25` كل فحص رغم اكتمال 233/233) كانت تُحسب «متأخرة» زورًا.
> ذاكرة الدورات المغلقة (`_recent` بسعتها القائمة) صارت تحمل وحدات كل دورة المساهمة:
> صدى مساهمٍ → `echoes` حميد؛ متأخّر لم يساهم → `late` يُعدّ. نفس علاج 150/350/359.

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `1.2.0` | **محاسبة الغياب بالاسم + مبدأ الحداثة بالصحّة** (كانت `late` بالآلاف بلا أي قدرة على معرفة أي وحدة تفوّت الدورات): عدّادات مكتمل/منتهي-مهلة/مستبدَل + عدّاد غياب لكل وحدة (`missing_by_unit`) والرسالة تعلن `top_missing` — «مَن يجوّع الثقة» يُقرأ من اللوحة مباشرة. والصحّة تحكم على **الجديد منذ آخر فحص** لا على تراكم العمر (نفس إصلاح 617 v3.0.1 المقيس — متأخّرة واحدة قديمة كانت تُبقيها متعثّرة للأبد). كذلك أُصلحت حرّاسها: كانت تبني هويّة الدورة ببانٍ محلّي قديم (٣ مقاطع) يخالف عقد المصدر الموحّد الخماسي (ورقة الحلول §٣) فتسقط منذ اعتماده — صارت تشرب من `cycle_key_of` حصرًا (٦/٦). ملاحظة: `manifest` كان `1.1.1` و`atom.py` كان `1.1.0` — انحراف موروث وُحِّد على `1.2.0`. |
| ٢٠٢٦-٠٨-١٨ | `1.1.1` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 21:29`

## 1.2.3 — 2026-08-20
- تنظيف مادة اللغة — الكود يتكلم رموزًا إنكليزية واللوحة تترجم؛ لا تغيير منطق. حُذفت تعليقات عربية فقط من atom.py؛ مضمونها مسجّل مسبقًا في هذا الملف.

## 3.0.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

## 3.0.1 — 2026-08-23
- مواءمة نسخة المانيفست مع `ATOM_VERSION` في الكود (كانت المانيفست متأخّرة — المدقّق الرسميّ: نسخة مزدوجة). لا تغيير في أي سطر سلوكي.

## 3.1.0 — 2026-08-23
**إقفال القسم 150 · مرحلة ١: «غير معرَّفة» تعبر البوابة كما هي.** البطاقة القادمة من 413 بثقة `null` تمر `null` إلى خارج القسم مع `confidence_defined: false` وسبب `CONFIDENCE_UNDEFINED` — بعدما كانت تُحوَّل إلى صفر كاذب. وأُضيفت `readiness_pct` المتدرجة. البوابة لم تخفّ: نفس عتبات الـ60 حرفيًا.


## 3.2.0 — 2026-08-23
**إقفال القسم 400 «الاستراتيجيات» · ذرّات أولًا (أمر المالك) — آخر الأقسام الستة.**

- **مفتح دورات التِكّة أُسكِت** (مسار فتح الوحدات كان موجودًا أصلًا): الدورة تُفتح من أول تسليم استراتيجية بهويتها وتكتمل 12+المجمّع طبيعيًا.
- **المهلة صارت تدفع الجزئي معلنًا** (`complete:false` + قائمة الناقص) — بعدما كانت تحذف الدورة صامتة.
- **حدث جديد `strategy.units.state`:** كل استراتيجية والمجمّع يعلنون أنفسهم — ما قالوا · ثقتهم (و«غير معرّفة» تمر `null` معلنة من مرحلة ١) · أوزانهم · تسليماتهم · عمر آخر تسليم · إيقاعهم ووقتهم الجاي.
- عدّاد الفتح صار صادقًا (كان يعدّ كل تِكّة حتى للدورات القائمة).
- الاختبارات: **4/4** (منها لوحة الوحدات والمهلة الجزئية).

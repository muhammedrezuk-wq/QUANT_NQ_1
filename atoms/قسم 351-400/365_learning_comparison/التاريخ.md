# التاريخ — 365_learning_comparison

**الذرّة:** `٣٦٥` · **الإصدار:** `1.0.4`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-٢٤ | `1.0.4` | إصلاح عطل إقلاع حرج (مقاس بالنواة الحيّة): جسد `__init__` و`initialize` كان مُسقَطًا على مستوى الجسم الكلاسي (أسلوب مضغوط) ف`self._running=False` يُنفَّذ وقت تعريف الصنف ⇒ `NameError: name 'self' is not defined` ⇒ المُقلِع يستبعد الذرة 365 من الإقلاع (`تعذّر تحميل الذرة 365`)، واختبارها يرسو خطأ جمع. أُعيدت الكتابة بأسلوب المشروع (حملة «صفر أسلوب مضغوط») — **صفر تغيير سلوكي**: نفس الأحداث والمنطق والعدّادات والصحّة. |
| ٢٠٢٦-٠٨-١٩ | `1.0.2` | تصحيح دلالة الصحّة بأمر المالك («ما بدي أشوف شي متعثّر أبدًا — مو كذب، حقيقة»): `comparisons=0` كان يُعلن `DEGRADED` رغم أن الذرّة تعمل سليمة ولم يصلها أي نموذج متحقَّق بعد (لا صفقات مغلقة بالنظام) — جاهزية قبل أول عمل، لا عجز فعلي. صارت `HEALTHY` برسالة «جاهز — بانتظار أول نموذج متحقَّق للمقارنة | comparisons=0» مع بقاء العدّادات في التفاصيل. `NOT_STARTED` تبقى `UNHEALTHY`. صفر تغيير سلوكي آخر. |
| ٢٠٢٦-٠٨-١٩ | `1.0.1` | أضيف `name_ar: "مقارنة نماذج التعلّم"` — كانت هذه من `٩` ذرّات (`360`-`368`) الاستثناء الوحيد بالمشروع بلا اسم عربي صريح، فطلع اسمها الإنكليزي الخام باللوحة. ترجمة مأخوذة حرفيًّا من `سياق/٩٧`. صفر تغيير سلوكي. |
| ٢٠٢٦-٠٨-١٨ | `1.0.0` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 12:00`

## 1.0.3 — 2026-08-20
- تنظيف مادة اللغة — الكود يتكلم رموزًا إنكليزية واللوحة تترجم؛ لا تغيير منطق. استُبدلت رسالة الصحّة: «جاهز — بانتظار أول نموذج متحقَّق للمقارنة | comparisons=0» ← `READY_AWAITING_FIRST_VALIDATED_MODEL | comparisons=0`

- **حملة ١–٤٤٩ · الدفعة ب (٢٠٢٦-٠٨-٢٣):** أُعلن حدّ الجوع `max_idle_s: 3600` في metadata — مشتق من إيقاع اشتراكات الذرّة، رقم كريم مؤقّت قابل لضبط المالك من اللوحة لاحقًا. صفر تغيير في الكود والسلوك.

- **حملة ١–٤٤٩ · الدفعة ب (٢٠٢٦-٠٨-٢٣):** فُصلت الأسطر متعددة الأوامر — صفر تغيير منطقي.

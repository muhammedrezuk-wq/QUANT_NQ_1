# التاريخ — 359_مجمّع_الثقة

**الذرّة:** `٣٥٩` · **الإصدار:** `1.3.1`

| التاريخ | الإصدار | ما تغيّر |
|---|---|---|
| ٢٠٢٦-٠٨-١٩ | `1.3.0` | **علاج مرضَي الصحّة المقيسَين** (قياس حيّ قبل الإصلاح: `late=18` ثابت عبر دقائق القياس — دفعة قديمة كانت تُبقي الذرّة «متعثّرة» للأبد رغم نظافة الحاضر): (١) **مبدأ الحداثة** — `health_check()` كانت تحكم على مجاميع العمر كلّه؛ صارت تحكم على **الجديد منذ آخر فحص** فقط (`_health_seen` ثم `new_X = total - seen`) والمجاميع باقية في التفاصيل — نفس نمط `400 v1.2.0` ومبدأ `617 v3.0.1` المقيسَين. (٢) **تمييز الصدى** — ذاكرة الدورات المغلقة `_closed` (نفس السعة `_MAX_CLOSED=256`، لا رقم جديد) صارت قاموسًا يحمل مجموعة النماذج المساهمة لكلّ دورة بدل المعرِّف وحده: صدى نموذج ساهم بدورة أُغلقت حديثًا حميد (يُحصى في `echoes`، لا يمسّ الصحّة)، ومتأخّر حقيقي لنموذج لم يساهم يبقى `late`. تحسين لازم للتحويل: الإخلاء عند الامتلاء كان عشوائيًّا (`set.pop()`) وصار الأقدم-أولًا. أُضيف ٣ حرّاس (تعافٍ بعد حادثة قديمة · صدى لا يُعدّ · متأخّر حقيقي يُعدّ) — النتيجة ١٠/١٠. |
| ٢٠٢٦-٠٨-١٨ | `1.2.1` | إنشاء ملفّ التاريخ · تنظيف الحشو من `atom.py` و`manifest.yaml` (تعليقات وشروحات فقط) — **صفر تغيير في السلوك**، مُثبَت بمقارنة الشجرة النحويّة |

> **ما قبل هذا التاريخ غير مسجّل.** لم يكن للذرّة ملفّ تاريخ، ولا يُخترع تاريخ لم يُقس.
> آخر تعديل على `atom.py` قبل التنظيف: `2026-08-17 21:29`

## 1.3.1 — 2026-08-20
- تنظيف مادة اللغة — الكود يتكلم رموزًا إنكليزية واللوحة تترجم؛ لا تغيير منطق. حُذفت تعليقات عربية فقط من atom.py؛ مضمونها مسجّل مسبقًا في هذا الملف.

## 2.0.0 — 2026-08-23
- تحديث توثيقي فقط: أُعيد توليد بطاقة الشرح من `manifest.yaml` الحالي (أحداث النشر والاشتراك والإصدار). لا تغيير في الكود ولا في السلوك.

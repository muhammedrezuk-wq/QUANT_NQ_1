# Learning Trainer

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٣٦٣` |
| الاسم | Learning Trainer |
| المجلّد | `363_learning_training` |
| الإصدار | `1.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `learning.feature.ready`

**تُرسِل (من `manifest.yaml`):**
- `learning.model.candidate`

**إعداداتها:** `min_samples` · `validation_size` · `train_every_new_samples` · `epochs` · `learning_rate` · `l2`

<!-- BITAQA_END -->

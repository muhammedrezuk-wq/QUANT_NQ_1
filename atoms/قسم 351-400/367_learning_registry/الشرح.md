# Learning Model Registry

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٣٦٧` |
| الاسم | Learning Model Registry |
| المجلّد | `367_learning_registry` |
| الإصدار | `1.3.1` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `learning.model.selected`
- `model.persisted`
- `storage.persistence.load_response`
- `learning.model.rollback_requested`
- `market.tick.validated`

**تُرسِل (من `manifest.yaml`):**
- `learning.model.active.state`
- `learning.model.evidence`
- `model.persist_requested`
- `storage.persistence.load_requested`

**إعداداتها:** `mode` · `rollback_cooldown_seconds`

<!-- BITAQA_END -->

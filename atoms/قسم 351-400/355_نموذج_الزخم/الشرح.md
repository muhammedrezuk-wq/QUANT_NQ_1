# Tick Momentum Probability Model

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٣٥٥` |
| الاسم | Tick Momentum Probability Model |
| المجلّد | `355_نموذج_الزخم` |
| الإصدار | `2.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.tick.validated`

**تُرسِل (من `manifest.yaml`):**
- `probability.momentum.state`

**إعداداتها:** `weight` · `required_depth` · `confidence_threshold` · `strength_threshold` · `required_ticks` · `tick_window`

<!-- BITAQA_END -->

# Tick Pullback Probability Model

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٣٥٤` |
| الاسم | Tick Pullback Probability Model |
| المجلّد | `354_نموذج_الارتداد` |
| الإصدار | `2.0.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `market.tick.validated`

**تُرسِل (من `manifest.yaml`):**
- `probability.pullback.state`

**إعداداتها:** `weight` · `required_depth` · `confidence_threshold` · `strength_threshold` · `required_ticks` · `tick_window` · `pullback_min_pct`

<!-- BITAQA_END -->

# Learning Drift And Rollback

> أُعيد توليده آليًا بعد الدمج الموحّد (نافذة 2026-08-28) — محتواه مقيس من `manifest.yaml` المدمج.

<!-- BITAQA_START -->

> **بطاقة مقيسة — مقروءة من `manifest.yaml` و `atom.py` يوم ٢٠٢٦-٠٨-٢٨. لا تُكتب باليد.**

| | |
|---|---|
| الرقم | `٣٦٨` |
| الاسم | Learning Drift And Rollback |
| المجلّد | `368_learning_drift` |
| الإصدار | `1.1.0` |
| حرجة؟ | `false` |
| الإقلاع | `auto` |
| فحص الصحّة | كل `١٠` ثانية |

**تسمع (من `manifest.yaml`):**
- `learning.outcome.recorded`
- `learning.model.active.state`

**تُرسِل (من `manifest.yaml`):**
- `learning.model.drift.state`
- `learning.model.rollback_requested`

**إعداداتها:** `min_samples` · `min_accuracy` · `window_size` · `rollback_cooldown_seconds`

<!-- BITAQA_END -->
